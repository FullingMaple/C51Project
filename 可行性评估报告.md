# 可行性评估报告：STC8H8K64U 实验箱 9.6 电子手表式 UI 菜单（0.96" OLED 版）

> 评估日期：2026-08-14（v3：实体屏已点亮验证，补充接线方案与虚拟 OLED 调试路径）
> 评估对象：[项目介绍.md](实训项目/项目介绍.md) 中规划的电子手表风格多功能 UI 菜单系统
> 屏幕：**0.96" SSD1306 OLED（128×64，软件 I2C）—— 2026-08-14 已实测点亮**
> 评估依据：SSD1306 实测驱动经验、STC 官方例程（DEMO-CODE-V9.6）、上游 OLED_UI 框架（bdth-7777777）、GitHub 开源资料检索、实验箱硬件资源核对

---

## 1. 总体结论

**题目成立，方案成立，且性能从"够用"升级为"富余"。**

改用 SSD1306 OLED 后，刷新瓶颈从 ST7920 的"控制器指令执行速度"（~72µs/字节，全屏 12fps 上限）变为 I2C 总线速度（~10~24ms 全屏，40~106fps）——**所有原方案中被屏幕帧率限制的功能（全屏动画、动作游戏）全部解锁**。同时：

- 驱动层工作量**下降**：上游 OLED_UI 原生支持 SSD1306 IIC，且综合实训工程已有实测点亮的 C51 软件 I2C 驱动（2026-08-10）
- 框架/字模/按键/应用层**全部复用**：v0.2 的 C51 移植成果几乎不受影响，只需替换底层驱动一个文件
- 原"亮度设置"占位功能变真功能：SSD1306 0x81 对比度寄存器真实可调（ST7920 实验箱背光硬接电源无法调）

---

## 2. 屏幕方案要点（SSD1306 0.96" OLED）

### 2.1 硬件连接（已实测验证）

| 项目 | 配置 | 依据 |
|---|---|---|
| 接口 | 软件 I2C：SCL=P2.5、SDA=P2.4 | 综合实训工程 2026-08-10 实测点亮 |
| 地址 | 0x78（0x3C<<1）；模块焊盘改 0x7A 侧则改 OLED_ADDR | 同上 |
| 引脚模式 | P2.2~P2.5 漏极开路（P2M1/P2M0=0x3c），实验箱外部上拉到 3.3V | 官方例程 IO 配置 |
| 供电 | 3.3V（实验箱提供） | 模块标准供电 |

### 2.2 实验箱接线实操要点（2026-08-14 实物确认）

- **J10（4 针 I2C 座）出厂未焊**：实验箱上 OLED 接口区域只有 7 孔座（J11），J10 的 4 针座是选配
- **7 孔座（J11）承载 I2C/SPI 双定义**（原理图：J11=SPI 7线、J11B=I2C 7线共用同一插座），I2C 模式孔位定义：**1=GND、2=VCC(3.3V)、3=SCL(P2.5)、4=SDA(P2.4)、5=RES、6=DC、7=CS**
- **模式电阻**：R173/R174（SPI，出厂默认）↔ R175/R176（I2C）——用 I2C 前必须把 R173/R174 挪到 R175/R176（或短接 R175/R176 焊盘），否则 7 孔座的 SCL/SDA 孔不导通
- **实际接法**（已验证）：
  - 方案 A：改焊电阻后，4 针模块按丝印 GND-VCC-SCL-SDA 直插 7 孔座 1~4 孔（孔距 2.54mm 对齐）
  - 方案 B：纯飞线，SCL/SDA 从 R175/R176 空焊盘取（P2.5/P2.4 侧），VCC/GND 从实验箱电源排针取
  - 安全：VCC/GND 反接必烧模块，插前用万用表量孔位电压确认
- **显示方向坑**：69 号例程初始化用 `0xC0`（上下翻转）→ 改 `0xC8` 正常；左右镜像改 `0xA0↔0xA1`。已实测点亮

### 2.3 驱动现状（三层保障）

1. **综合实训实测驱动**（`d:\Document\本科\单片机课程\综合实训\src\main.c`）：软件 I2C（Start/Stop/SendByte 带 ACK 检测）、OLED_WrCmd/WrDat/SetPos、Init（ACK 探测失败返回 0）、Clear/FillAll/ShowChar/ShowString/ShowUInt（6×8 字库）——**已点亮，可直接作底层底子**
2. **上游框架原生驱动**：[bdth-7777777/OLED_UI](https://github.com/bdth-7777777/OLED_UI) 的 OLED_driver.c 原生面向 SSD1306 IIC（README 已推出例程）——驱动层从"完全重写"降为"C51 适配"
3. 初始化序列要点（综合实训已验证）：0xA1 段重映射（镜像改 0xA0）、0xDA 0x12 COM 配置（半边亮改 0x02）、0x8D 0x14 电荷泵

### 2.4 虚拟 OLED 调试路径（无实体屏也可开发）

- **AiCube-ISP（V6.95Z+）**：`工具 → 调试仿真接口 → OLED-128*64`，PC 端渲染虚拟 OLED
- **机制**：MCU 经 USB-CDC 把整屏 1024 字节发给 PC；官方 69 号例程 `VirtualDevice` 开关切换虚拟/实体（默认 `=1` 虚拟）
- **对本项目**：驱动层做"实体 I2C / 虚拟 USB"双后端（`#if` 编译开关），开发期用虚拟屏调 UI 逻辑（菜单/动画/字模全可调），实体屏只验证驱动
- **注意**：虚拟屏 FPS 受 USB 传输限制，不代表真实性能；性能指标须实体屏验证

### 2.5 性能量化（128×64 全屏 = 1024 字节）

| 指标 | ST7920（原方案） | SSD1306 OLED（现方案） |
|---|---|---|
| 每字节写入 | ~72µs（等 BF 忙检测） | ~9~22µs（I2C 1MHz~400kHz，无忙检测） |
| 全屏刷新 | ~83ms → **12fps 上限** | **~10~24ms → 40~106fps** |
| 菜单高亮（2 行） | ~3ms | <0.5ms |
| 全屏淡入/滑动动画 | ❌ 不可行 | ✅ 40fps+ 流畅 |
| 动作游戏 | ⚠️ 显示上限 12fps | ✅ 无压力 |

### 2.6 与 ST7920 方案的关键差异

| 维度 | ST7920（原方案） | SSD1306 OLED（现方案） |
|---|---|---|
| 深浅色主题 | 0x0D/0x0C 硬件反色 | 0xA7/A6 反色指令，等效 |
| **亮度调节** | ⚠️ 背光硬接电源，只能占位 | ✅ 0x81 对比度寄存器，**真实可调** |
| 内置字库 | CGROM 8192 中文 | 无 → 全部自建字模（已有 GB2312 12×12/16×16，复用） |
| 物理尺寸 | ~3 寸大屏 | 0.96 寸小屏（布局需微调，16×16 中文单行 8 字） |
| 实验箱集成 | 自带屏 | 外接模块（飞线 2 根，P2.4/P2.5） |
| 引脚占用 | 数据口 P6 + 5 根控制线 | 仅 2 根（I2C） |
| diff 刷新引擎 | 必需（12fps 下唯一方案） | 保留，锦上添花（局部刷新 <0.5ms） |
| 技术亮点 | diff 引擎（硬核展示） | 动画流畅 + 手表风格效果更贴题 |

---

## 3. 逐功能效果评估（OLED 版）

| 功能 | 可行性 | 预期效果 |
|---|---|---|
| 磁贴桌面 + 选择框动画 | ✅ | 局部刷新 <0.5ms，**丝滑** |
| 多级菜单 + 窗口组件 | ✅ | 局部刷新，丝滑 |
| 滑动/淡入动画 | ✅ | **全屏 40fps+，任意动画可行**（原方案被 12fps 卡死的全部解锁） |
| 深浅色主题 | ✅ | 0xA7/A6 硬件反色，即时切换 |
| **亮度调节** | ✅ | **0x81 对比度真实可调**（从占位升级为真功能，窗口+数据条 UI 不变） |
| 设置 + EEPROM | ✅ | 退出时统一写一次，无感 |
| NTC 测温 | ✅ | 1s 采样 + 数字区局部刷新 |
| 串口页 | ✅ | 终端式区域滚动 |
| 计算器 | ⚠️ 注意 | 16 键映射与菜单 4 键冲突，需"直读 KeyCode 1~16"模式；MDU16 可加速 |
| 贪吃蛇 | ✅ | 每帧刷 2 格，远低于帧预算 |
| 俄罗斯方块 | ✅ | 消行动画也流畅（原方案消行 ~10ms 有压力） |
| 小恐龙 | ✅ | 地面带按列刷新 + 全屏能力富余，**从"打折扣"变流畅** |
| 实时时钟 | ✅ | Timer 累计，简单 |
| **帧率仪表盘** | ✅ | 6×8 小字实时显示，OLED 上可达 40fps+ 真实统计 |

---

## 4. 关键风险与对策（OLED 版）

1. **0.96 寸物理屏小**：26.9×13.4mm 显示区，演示观感不如 3 寸 LCD 气派 → 布局按小屏优化（磁贴 32×32 半屏 4×2 仍成立；菜单行高 ≥16px 保证可读性），答辩时附 ST7920 对比演示
2. **I2C 速率依赖代码质量**：软件 I2C 若只跑 100kHz，全屏 ~92ms 回到 ST7920 水平 → 位操作内联优化至 400kHz~1MHz；SSD1306 无忙检测，速率只受总线时序约束
3. **中文字模分页写入**：SSD1306 按 8 像素页寻址，12×12/16×16 字模跨页需按页截取 → 图形层已有处理（上游 OLED 库原生支持 SSD1306），移植时验证
4. **计算器/游戏键位复用**：菜单态 4 键 → 应用态 16 键的切换是唯一新设计的交互层 → 框架层预留双通道接口（`UI_GetKey()` + `UI_GetRawKey()`）
5. **代码体积**：v0.2 已实测 53KB / 64KB，字模是主要占用 → 新增功能前确认余量（OLED 方案与 ST7920 方案体积相近，无新增压力）
6. **实体屏接线**：J10 4 针座出厂未焊；I2C 模式需改焊 R175/R176；VCC/GND 反接必烧模块 → 插前万用表量孔位电压

---

## 5. 建议

1. **开发路径：虚拟屏先行、实体屏后验**——驱动层做双后端（`#if VIRTUAL_OLED`），开发期用 AiCube-ISP 虚拟 OLED 调全部 UI 逻辑（无实体屏依赖），实体屏已点亮验证，随时可切换
2. **驱动层以 69 号例程 + 综合实训实测驱动为底子**，按上游 OLED_UI 的 OLED_driver 接口适配（framebuffer 1KB xdata 结构不变）
3. **其余分层全部复用 v0.2 成果**：图形层/字库/UI 核心/ADC 键盘/Timer 均不动，只改驱动与接线配置
4. 里程碑：v0.3 屏幕切换收尾（虚拟/实体双后端 + 亮度真功能）→ v0.4 设置 EEPROM → v0.5 测温 → v0.6 串口 → v0.7 计算器 → v1.x 游戏（贪吃蛇 → 俄罗斯方块 → 小恐龙，**全部可达流畅**）
5. 键位双通道接口在 v0.3 前预留（见第 4 节）
6. 实体屏接线按 2.2 节方案 A/B，插前万用表量孔位电压（VCC/GND 反接必烧）

---

## 6. 参考来源

- [bdth-7777777/OLED_UI（上游框架，原生支持 SSD1306，Apache-2.0）](https://github.com/bdth-7777777/OLED_UI)
- [forntoh/LcdMenu（Arduino ST7920 菜单库，概念参考）](https://github.com/forntoh/LcdMenu)
- 69 号例程（I2C OLED 示波器，`VirtualDevice` 双后端参考，2026-08-14 实体屏点亮）
- 综合实训工程实测驱动（SCL=P2.5、SDA=P2.4、0x78，2026-08-10 点亮）
- STC 官方例程 DEMO-CODE-V9.6（本地 `D:\edge_download\STC8H8K64U-DEMO-CODE-V9.6\`，含 17-ADC键盘 / 18-NTC / 19-EEPROM / 46-MDU16）
- 实验箱原理图 `SCH_实验箱9.62A_2026-1-27.pdf`（J10/J11 接口、R173-R176 模式电阻）
- [STC8H8K64U-TestBox-V9.62 立创商城资料页](https://item.szlcsc.com/product/jpg_54484558.html)
- [STC 官方论坛 - 实验箱 OLED 使用经验](http://www.stcaimcu.com/forum.php?mod=viewthread&tid=495&page=1&authorid=17)
- STC 官方手册 MCP 服务（`https://help.stcaimcu.com/mcp`，已挂载本工程 `.vscode/mcp.json`）

---

## 附：与 v1 版（ST7920）报告的差异小结

| 变化 | v1（ST7920） | v2（SSD1306 OLED） |
|---|---|---|
| 全屏刷新率 | 12fps 上限 | 40~106fps |
| 全屏动画 | 不可行 | 可行 |
| 动作游戏 | 可玩级（打折） | 流畅 |
| 亮度设置 | 占位 | 真功能 |
| 驱动工作量 | 完全重写（中） | 参照移植（小~中） |
| 技术亮点 | diff 引擎（硬核） | 效果流畅 + 手表风格贴题 |
| 新代价 | — | 屏小、外接、无内置字库 |
