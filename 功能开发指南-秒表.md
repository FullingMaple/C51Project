# 秒表功能开发指南（AI 协作版）

> 前置：先读 `功能开发指南-计算器.md` 的 §0（工作方式 + 分支铁律 + Skill 用法）和 §2（环境速通——虚拟屏/构建/烧录），本指南只讲秒表本身。

## 1. 任务总览

| 项 | 内容 |
|---|---|
| 功能 | 主界面"秒表"磁贴（第 3 位）→ 秒表页：0.1s 精度计时 + 暂停/继续 + 记录 + 复原 |
| 输入 | 物理键 1/2/3/4 + 遥控 ▲▼/OK/◀（映射一致） |
| 显示 | 虚拟 OLED 屏（VIRTUAL_OLED=1） |
| 验收 | §5 验证清单（8 项全过） |

**你只允许改动 3 个文件**（其余一律不许动）：

| 文件 | 改动 | 编码 |
|---|---|---|
| `src/ui/OLED_UI/OLED_UI_MenuData.c` | 秒表页（页面/绘制/按键/状态机/记录） | **GBK** |
| `src/ui/Driver/Hardware_Driver/OLED_UI_Driver.c` | 全局计时驱动（Stopwatch_Tick 0.1s 计数） | **UTF-8** |
| `src/ui/Driver/Software_Driver/OLED_Fonts.c` | 补字模（用 add_fonts.py，不要手改） | **GBK** |

> ⚠️ **文件编码不同**：MenuData.c / OLED_Fonts.c 是 GBK，OLED_UI_Driver.c 是 UTF-8——AI 改哪个文件用哪个编码，改完验证中文没乱码。**GBK 文件禁止用文本编辑工具直接改**（用 python rb/wb 或 add_fonts.py）。

## 2. 验收标准（先看目标长什么样）

```
┌────────────────────────────┐
│      00:00:00.0            │  ← 8×16 大字号居中（时:分:秒.0.1s）
│                            │
│   01 00:00:05.3            │  ← 记录区（8×6 小字，最新 3 条）
│   02 00:00:04.1            │
│   03 00:00:02.0            │
│  1暂停 2记录 3复原         │  ← 底部提示（6×8，随状态切换）
└────────────────────────────┘
```

**交互**（状态机：初始 → 计时中 → 暂停中）：
- **初始态**：显示 00:00:00.0，无记录；键 1 = 开始计时
- **计时中**：大数字走时；键 1 = 暂停、键 2 = 记录（记当前 0.1s 值，最多 8 条环形覆盖）、键 3 = 无效、键 4 = 退出（**计时继续，全局状态**）
- **暂停中**：键 1/2 = 翻看记录（每页 3 条）、键 3 = 复原（归零 + 清空记录）、键 4 = 退出（**状态保留**）
- **任意状态退出页面 → 再进入**：显示保留的状态（计时中继续走、暂停中保持暂停、记录完整）——**全局状态，不是退出即复位**
- **归零/无特殊提示**：秒表无蜂鸣需求（按键音已有）
- 记录格式：`序号 时:分:秒.0.1s`（如 `01 00:05:03.2`），序号 01 最新

## 3. 任务书（复制给 AI）

```
# === 任务书开始 ===

你是 STC8H8K64U 单片机的 C51 嵌入式工程师。项目是"电子手表风格 UI 菜单系统"
（OLED_UI C51 移植框架，Keil C51，显示走虚拟 OLED）。
你的任务：实现秒表页面。所有代码 C51 兼容（C89 子集）。

## 背景（必须先了解）
- 框架已提供：Key_GetRawKey()（返回键码 1~16，0=无键；含遥控逻辑键）；
  OLED_Printf/OLED_ShowString/OLED_ShowMixString/OLED_ReverseArea/OLED_Clear；
  OLED_8X16_HALF（8×16 ASCII）、OLED_6X8_HALF（6×8 ASCII）、
  OLED_12X12_FULL（12×12 汉字）、OLED_7X12_HALF（7×12 ASCII）
- 页面模型：0 菜单项页（MenuItems 只有 {NULL}）+ 每帧回调 General_ShowAuxiliaryFunction
- 已有范例：src/ui/OLED_UI/OLED_UI_MenuData.c 里的 TimeAuxFunc（时间页）——
  0 菜单项页面怎么建、怎么在 AuxFunc 里读键/绘制/反色，照它写
- Timer0 20ms 中断在 OLED_UI_Driver.c（Driver_TickHandler 每 20ms 调一次）

## 任务 1：全局计时驱动（OLED_UI_Driver.c）
秒表必须全局计时（退出页面继续走）。在 OLED_UI_Driver.c 新增：
- 全局变量（必须显式初始化 =0，工程无 STARTUP.A51）：
  uint8_t  SW_Running;   /* 0=初始 1=计时中 2=暂停中 */
  uint32_t SW_Tenths;    /* 0.1s 计数 */
  uint32_t SW_Records[8];/* 记录（0.1s 值），环形 */
  uint8_t  SW_RecCount;  /* 已记录条数 0~8 */
  uint8_t  SW_RecIdx;    /* 最新记录下标（环形） */
- 在 Driver_TickHandler()（或 Timer0 ISR 内新函数）里：
  if(SW_Running == 1){ SW_Tenths++; }   /* 每 5 tick = 0.1s（20ms×5=100ms） */
  注意：Driver_TickHandler 每 20ms 调用——每 5 次加 1 → 需要自己的 5 分频计数
- 头文件 OLED_UI_Driver.h 加 extern 声明

## 任务 2：秒表页（MenuData.c）
1. 磁贴：MainMenuItems 插入 {"秒表", NULL, &StopwatchMenuPage, NULL, NULL, NULL, Image_stopwatch, NULL, 0, 0}
   （第 3 位，设置/时间/秒表/测温/串口/计算器/游戏；Image_stopwatch 图标见任务 3）
2. 页面：MenuPage StopwatchMenuPage = { MENU_TYPE_LIST, SPEED, NOT_SHOW, UNLINEAR,
   OLED_UI_FONT_12, &MainMenuPage, StopwatchMenuItems(0 项), 0, StopwatchAuxFunc, {0,0,128,64}, ... }
3. StopwatchAuxFunc 每帧：
   a. 读键：k = Key_GetRawKey(); 边沿去重（上次键值 static，k!=0 且上次==0 才处理）
   b. 状态机：
      - SW_Running==0（初始）：键 1 → SW_Running=1
      - SW_Running==1（计时中）：键 1 → SW_Running=2（暂停）；键 2 → 记录
        （SW_Records[SW_RecIdx]=SW_Tenths; SW_RecIdx=(SW_RecIdx+1)&7; 满 8 环形；
        SW_RecCount<8 则 ++）；键 3 → 无操作；键 4 → 页面退出（框架 Back）
      - SW_Running==2（暂停中）：键 1/2 → 翻看记录（static 翻页偏移，每页 3 条，
        从最新往前翻，共 SW_RecCount 条）；键 3 → 复原（SW_Tenths=0; SW_RecCount=0;
        SW_RecIdx=0; SW_Running=0 回初始态）；键 4 → 退出
   c. 绘制：
      - 大数字：OLED_Printf(居中x, 4, OLED_8X16_HALF, "%02d:%02d:%02d.%d", ...)
        从 SW_Tenths 换算 时/分/秒/0.1s：h=SW_Tenths/36000; m=(SW_Tenths/600)%60;
        s=(SW_Tenths/10)%60; d=SW_Tenths%10 —— 注意 %02d 的 uint8_t 参数必须 (int) 强转！
      - 记录区：y=24 起 3 行 8×6 小字，显示当前页 3 条（最新在前）：
        OLED_Printf(8, y, OLED_6X8_HALF, "%02d %02d:%02d:%02d.%d", ...)
      - 底部提示（6×8 居中）随状态：
        初始/计时中："1暂停 2记录 3复原"（汉字 12×12 + 数字 6×8 用 ShowMixString）
        暂停中："1/2翻页 3复原"
      - 提示文字用 OLED_ShowMixString（12×12 汉字 + 6×8 ASCII 混排）
4. 补字（任务 3 完成后）：12×12 需补 秒 表 暂 停 记 录 复 原 翻 页
   （"开始"不需要——秒表无"开始"文字提示；如提示文案不同按实际用字补）

## 任务 3：补字模 + 图标（用脚本，勿手改）
- 12×12 补字：python add_fonts.py 秒 表 暂 停 记 录 复 原 翻 页
- 16×16 补"秒 表"（磁贴名）：用 add_fonts16_icon.py 或让主开发补
- 图标：Image_stopwatch（32×32 秒表样式：圆盘+双按钮+指针，参考 Image_clock 画法）
  加到 OLED_Fonts.c + OLED_Fonts.h extern

## C51 铁律（违反会编译失败/显示错乱）
1. 变量声明必须在函数/块开头（C141）；for 初始化区不能声明变量
2. 变参 printf（OLED_Printf/PrintfMix）里 uint8_t 参数必须 (int) 强转（%d 读 2 字节、
   char 压 1 字节会粘连）
3. 所有全局/static 变量必须显式初始化 =0（无 STARTUP.A51，不初始化=随机值）
4. GBK 文件（MenuData.c/OLED_Fonts.c）禁止文本编辑器直接改——python rb/wb 同编码
5. 页面切换动画/渐隐由框架处理，页面内不要动 CurrentMenuPage

# === 任务书结束 ===
```

## 4. 补字与资源预算

| 项 | 内容 |
|---|---|
| 12×12 补字 | 秒 表 暂 停 记 录 复 原 翻 页（9 字 × 26B = 234B） |
| 16×16 补字 | 秒 表（2 字 × 34B = 68B，磁贴名） |
| 图标 | Image_stopwatch 32×32（128B） |
| 代码/数据 | ~1.0-1.5KB |
| **合计** | **~1.5-2.0KB**（当前预算剩 18.4KB，充足） |

> 补字前先查字库：`python -c "读 OLED_Fonts.c 的 Index 比对"`——缺字才补，避免重复。

## 5. 验证清单（全过才算完成）

- [ ] 1. 主菜单出现"秒表"磁贴（第 3 位），图标正常
- [ ] 2. 进入秒表页：显示 00:00:00.0，底部提示"1暂停 2记录 3复原"
- [ ] 3. 键 1 开始计时：0.1s 精度走时（观察 0.1s 位跳动），10 秒误差 <0.2s
- [ ] 4. 计时中键 2 记录：底部出现记录行（序号+时间），连记 5 条显示最新 3 条
- [ ] 5. 计时中键 1 暂停：数字停走；键 1/2 翻看记录（每页 3 条）
- [ ] 6. 暂停中键 3 复原：归零 + 记录清空 + 回初始态
- [ ] 7. **退出页面再进入**：计时中退出 → 再进时间继续走；暂停中退出 → 再进保持暂停+记录
- [ ] 8. 遥控 ▲▼/OK/◀ 操作与物理键一致；按键音正常
- [ ] 9. 构建 0 Error / 不超过 2 Warning；内存 Flash 总量 < 50KB
- [ ] 10. 提交（分支上）后通知主开发合入验证实体屏
