# NTC 测温功能开发指南（AI 协作版）

> 给你的任务：在现有项目中实现 **NTC 测温页面**。
> **你不写代码**——AI 写，你负责：给 AI 派活 → 监督改动 → 虚拟屏验证 → 提交。
> 预计 1-2 天。有任何不懂的术语，直接问 AI（把本指南发给它）。

---

## 0. 工作方式（先读这个）

```
循环：给 AI 任务书（§4） → AI 改代码 → 你检查（§5）→ 构建 → 虚拟屏验证（§6）
      → 有问题把现象回喂 AI → 循环，直到验证清单全过 → 提交（§9）
```

- AI 是 VSCode 里的 Claude 插件（DeepSeek 模型），能直接改文件、跑命令
- **每次 AI 改完代码后，你都要：① 看它改了哪些文件（§5 监督清单）② 构建 ③ 虚拟屏看效果**
- 不信任 AI 的"我觉得没问题"——一切以**构建通过 + 虚拟屏实测**为准

---

## 1. 任务总览

| 项 | 内容 |
|---|---|
| 功能 | 主界面"测温"磁贴 → 进入测温页：显示温度 + ADC 原始值 |
| 输入 | 实验箱 P1.3 上的 NTC 热敏电阻（MF52 10K@25°C，B=3950） |
| 显示 | 虚拟 OLED 屏（VIRTUAL_OLED=1，画面在 STC-ISP 窗口里） |
| 验收 | 见 §6 验证清单（8 项全过） |

**你只允许改动 2 个文件**（其余文件一律不许动！）：

| 文件 | 改动 |
|---|---|
| `src/ui/OLED_UI/OLED_UI_MenuData.c` | 加测温页代码（AI 做） |
| `OLED_Fonts.c`（`src/ui/Driver/Software_Driver/`） | 补 4 个字模（跑脚本，见 §4 步骤 2） |

---

## 2. 环境速通（10 分钟，只需一次）

### 2.1 切换到虚拟屏模式

用 VSCode 打开 `src/inc/config.h`，找到：

```c
#define VIRTUAL_OLED    0
```

改成 `1`（保存）。**这是虚拟屏总开关**——你开发期间一直保持 1。

### 2.2 构建（每次改完代码都执行）

在 VSCode 终端（Git Bash）里运行：

```bash
"D:/APP/C51V961/UV4/UV4.exe" -b "Project.uvproj" -o "build_log.txt" && sleep 25 && grep -E "error|Program Size" build_log.txt
```

**成功**：看到 `0 Error(s)` 和 `Program Size: ...`。
**失败**：把 `build_log.txt` 里的报错整段复制给 AI，让它修。

### 2.3 烧录 + 开虚拟屏

1. 打开 **STC-ISP**，载入 `Objects\Project.hex`，下载到实验箱
2. 打开 **STC-ISP → 工具 → 调试仿真接口 → OLED-128*64** 窗口
3. 画面实时显示在这个窗口里（**这个窗口必须一直开着**——关了 MCU 会等 USB 而死等）

> 虚拟屏常见问题：窗口开了没画面 → 先看 STC-ISP 的串口/下载是否正常；重新烧录后窗口自动刷新。

---

## 3. 验收标准（先看目标长什么样）

```
┌────────────────────────────┐
│       温度仪                │  ← 16×16 标题（顶部居中）
│                            │
│        25.3                │  ← 8×16 大数字（屏幕中间，只显示数值）
│                            │
│   当前ADC值: 1024           │  ← 12×12 文本
│                            │
│    [返回]                  │  ← 菜单项（底部，光标）
└────────────────────────────┘
```

- 温度值：1 位小数（`25.3`），室温约 20-30；**手摸 NTC 探头温度值上升**，松开回落
- ADC 值：12 位原始值（0~4095），与温度**反向**（温度越高 ADC 越小——NTC 上拉接法）
- NTC 断开/短路时温度显示 `--`（不是乱数字）
- 每 1 秒刷新一次

---

## 4. 给 AI 的任务书

**把下面整段复制给 AI**（`# ===` 之间的内容）：

```
# === 任务书开始 ===

你是 STC8H8K64U 单片机的 C51 嵌入式工程师。项目是"电子手表风格 UI 菜单系统"
（OLED_UI C51 移植框架，GBK 编码，Keil C51 编译，显示走虚拟 OLED）。
你的任务：实现 NTC 测温页面。所有代码 C51 兼容（C89 子集）。

## 背景（必须先了解）
- 工程：Project.uvproj；入口 src/app/main.c；菜单数据 src/ui/OLED_UI/OLED_UI_MenuData.c
- 框架：菜单项 MenuItem / 页面 MenuPage（MENU_TYPE_LIST 类型），测温磁贴
  MainMenuItems[2]（文本"测温"，图标 Image_thermo）当前子菜单为 NULL
- 显示 API（src/ui/Driver/Software_Driver/OLED.h）：
  OLED_ShowChinese(x, y, "中文", OLED_16X16_FULL) —— 中文
  OLED_ShowNum / OLED_ShowString —— ASCII 数字/字符
  OLED_PrintfMix(x, y, 中文字号, ASCII字号, "格式", ...)
- 时钟：GetTick()（毫秒，src/ui/Driver/Hardware_Driver/OLED_UI_Driver.h）
- ADC：键盘驱动已初始化 ADC（ADC_CONTR 电源已开，12 位，ADCCFG=0x2f）

## 任务：在 OLED_UI_MenuData.c 中实现测温页
只允许修改这一个文件！其他文件（含 OLED_Fonts.c）一律不动。

### 界面布局（128×64 屏）
1. 顶部："温度仪" 3 个 16×16 中文居中（x=(128-48)/2=40, y=2）
2. 中部：温度值 8×16 ASCII 大数字居中（y=20），1 位小数格式如 "25.3"；
   采样异常时显示 "--"
3. 下部："当前ADC值: 1024"（12×12 中文+ASCII，x=8, y=40）
4. 底部：[返回] 菜单项（菜单区域 List_MenuArea = {0, 52, 128, 12}）

### 实现要求
0. 先读文件：打开 OLED_UI_MenuData.c，**参考现有 SettingsMenuPage / AboutThisDeviceMenuItems 的结构体写法**（字段顺序、{NULL} 结尾、MenuPage 各字段），照抄模式。
1. 页面结构：
   - MenuItem NtcMenuItems[]：1 项 {"[返回]", OLED_UI_Back, ...} + {NULL} 结尾
   - MenuPage NtcMenuPage：MENU_TYPE_LIST，父页面 &MainMenuPage，
     General_ShowAuxiliaryFunction = 绘制回调，List_MenuArea 如上
2. 绘制回调（每帧被框架调用）：
   - 画标题/温度/ADC 值（用 OLED_ShowChinese / OLED_ShowNum / OLED_PrintfMix）
   - 温度每 1 秒采样刷新（GetTick 判断 1000ms），其余帧画上次值（diff 引擎自动省流量）
3. 采样：ADC 通道 3（P1.3）。参照键盘驱动模式：
   ADC_RES=0; ADC_RESL=0; ADC_CONTR=(ADC_CONTR&0xF0)|0x40|3; 4 个 _nop_();
   轮询 ADC_CONTR bit5（超时 ~1000 次返回 0xFFFF）
4. 温度换算：从"功能开发指南-NTC测温.md"附录复制 NTC_Table 数组（161 个 u16）
   和 NTC_GetTemp() 函数（二分查找+线性插值，返回 温度×10），
   按附录原样使用，不得改动数据
5. 异常处理：ADC>=4096（超时）或 ADC 值超出查表范围 → 温度显示 "--"
6. C51 铁律（违反会编译失败）：
   - 块内任意位置声明报错 → 变量声明放在函数开头
   - for 循环初始化区不能声明变量
   - 全局/static 变量必须显式初始化（如 = 0）！
   - 结构体初始化只能用位置初始化（C51 不支持 C99 命名初始化）
7. 文件编码：OLED_UI_MenuData.c 是 GBK——**你修改后必须保持 GBK 编码**，
   中文注释不要用特殊符号；改完告诉我"编码已保持 GBK"
8. 全局变量命名：Ntc_ 前缀（如 Ntc_Temp、Ntc_ADC、Ntc_Valid、Ntc_LastTick）

### 完成后
1. 运行构建命令，确保 0 Error（警告 L15 可接受，不用管）
2. 告诉我：改了哪些位置、新增了哪些函数/变量、构建结果
3. 不要提交 git（用户自己提交）

# === 任务书结束 ===
```

**任务书里提到的"附录"**：就是本指南的附录 A（往下翻），AI 需要从中复制查表数据。

---

## 5. 监督清单（AI 每轮改完你检查什么）

| # | 检查项 | 方法 |
|---|---|---|
| 1 | 只改了允许的文件 | 让 AI 展示改动文件列表（应只有 MenuData.c） |
| 2 | 构建 0 Error | 跑 §2.2 命令 |
| 3 | 中文没乱码 | 打开 MenuData.c 看中文注释是否正常（乱码=编码被破坏，让 AI 修复） |
| 4 | 代码特征抽查 | 让 AI 展示新增代码：确认有 `NTC_Table[161]`、`Ntc_` 前缀变量、页面结构体 |
| 5 | 补字脚本已跑 | 检查 `OLED_Fonts.c` 里是否出现"仪/度/当/前"4 个条目（步骤 2 后） |

---

## 6. 验证清单（虚拟屏上逐项打勾）

| # | 自测项 | 通过标准 |
|---|---|---|
| 1 | 主界面进入"测温"磁贴 | 渐隐动画 → 测温页正常显示 |
| 2 | 页面布局 | 顶部"温度仪"、中部温度、下部"当前ADC值"、底部 [返回] 齐全 |
| 3 | 温度值合理 | 室温显示 ~20-30（可对比手机天气/温度计） |
| 4 | 手摸 NTC 探头 | 温度值**上升**（约 1 秒内刷新） |
| 5 | 松开 | 温度值**回落** |
| 6 | ADC 值联动 | "当前ADC值"与温度反向（温度升 → ADC 值降） |
| 7 | [返回] | 返回主界面正常 |
| 8 | 反复进出 5 次 | 不卡死、不空白 |

**出现异常 → §7 处理。**

---

## 7. Bug 处理流程

```
现象（截图/描述）→ 复制给 AI："出现 XX 现象，屏幕显示 XX，请分析原因并修复"
→ AI 改代码 → 你重新构建 → 虚拟屏验证 → 循环
```

常见问题对照表：

| 现象 | 常见原因 | 让 AI 做什么 |
|---|---|---|
| 编译报错 C141/C248 | C51 声明位置/结构体初始化违规 | 按报错行号修，遵守 C51 铁律 |
| 页面空白/字缺失 | 字模没补 | 跑补字脚本（步骤 2） |
| 温度显示 0 或乱跳 | 采样/换算 bug | 打印 ADC 原始值对比 |
| 显示 `--` | 采样超时/通道不对 | 检查 ADC 通道号=3、引脚 P1.3 |
| 中文乱码（菜单全乱） | GBK 被破坏 | 立即 git 还原 MenuData.c，让 AI 用 python 按 GBK 处理 |
| 虚拟屏没画面 | STC-ISP 窗口没开/没烧录 | 检查 §2.3 |
| L15 警告 | 框架正常现象 | 忽略 |

**⚠️ 危险动作**（一旦出现立即 git 还原再继续）：
- AI 改了 MenuData.c 以外的文件 → `git checkout -- <文件名>` 还原
- MenuData.c 中文乱码 → `git checkout -- src/ui/OLED_UI/OLED_UI_MenuData.c` 还原
- 建议：**每次让 AI 动手前，先提交一次 git**（§9）——随时能回滚

---

## 8. 内存监控

每次构建看 `Program Size`：

```
Program Size: data=15.5  xdata=3808  const=6528  code=42760
```

- `code`：Flash 程序 + 常量。当前 42760（48.1KB/64KB），**测温功能完成后应在 44500 以内**（预计 +1.7KB 左右）
- `xdata`：RAM。当前 3808/8192，测温页只加几个全局变量（<50B）
- 超限（code > 63000 或 xdata > 8100）→ 立即告诉主开发

---

## 9. git 操作

**每次 AI 改完一版、验证通过后提交一次**（VSCode 终端）：

```bash
git add src/ui/OLED_UI/OLED_UI_MenuData.c src/ui/Driver/Software_Driver/OLED_Fonts.c
git commit -m "feat: NTC测温页（第N版）——XX 功能正常"
git push origin feature/ntc
```

- 只提交上面 2 个源码文件（**不要提交 hex/其他文件**）
- 提交信息格式：`feat: ...`（功能）/ `fix: ...`（修复）
- 分支：`feature/ntc`（你的专属分支，别动 dev）

---

## 10. 完成后

验证清单全过 → 在群里告诉主开发"NTC 测温完成" → 主开发负责：构建复查、实体屏验证、字库检查、合入 dev。你的任务结束。

---

## 附录 A：查表数据与换算函数（复制给 AI 用）

以下代码**原样复制**进任务书给 AI，或让 AI 从本附录读取。**数据不得改动**（温度-ADC 对照表，索引 0 = -40°C，索引 160 = +120°C）。

```c
/* 温度-ADC 查找表：索引 = 温度+40（0=-40°C，160=120°C） */
static const uint16_t code NTC_Table[161] = {
    140, 149, 159, 168, 178, 188, 199, 210, 222, 233,   /* -40 ~ -31°C */
    246, 259, 272, 286, 301, 317, 333, 349, 367, 385,   /* -30 ~ -21°C */
    403, 423, 443, 464, 486, 509, 533, 558, 583, 610,   /* -20 ~ -11°C */
    638, 667, 696, 727, 758, 791, 824, 858, 893, 929,   /* -10 ~ -1°C  */
    965, 1003, 1041, 1080, 1119, 1160, 1201, 1243, 1285, 1328,  /* 0 ~ 9°C */
    1371, 1414, 1459, 1503, 1548, 1593, 1638, 1684, 1730, 1775,  /* 10 ~ 19°C */
    1821, 1867, 1912, 1958, 2003, 2048, 2093, 2137, 2182, 2225,  /* 20 ~ 29°C */
    2269, 2312, 2354, 2397, 2438, 2479, 2519, 2559, 2598, 2637,  /* 30 ~ 39°C */
    2675, 2712, 2748, 2784, 2819, 2853, 2887, 2920, 2952, 2984,  /* 40 ~ 49°C */
    3014, 3044, 3073, 3102, 3130, 3157, 3183, 3209, 3234, 3259,  /* 50 ~ 59°C */
    3283, 3306, 3328, 3351, 3372, 3393, 3413, 3432, 3452, 3470,  /* 60 ~ 69°C */
    3488, 3506, 3523, 3539, 3555, 3571, 3586, 3601, 3615, 3628,  /* 70 ~ 79°C */
    3642, 3655, 3667, 3679, 3691, 3702, 3714, 3724, 3735, 3745,  /* 80 ~ 89°C */
    3754, 3764, 3773, 3782, 3791, 3799, 3807, 3815, 3822, 3830,  /* 90 ~ 99°C */
    3837, 3844, 3850, 3857, 3863, 3869, 3875, 3881, 3887, 3892,  /* 100 ~ 109°C */
    3897, 3902, 3907, 3912, 3917, 3921, 3926, 3930, 3934, 3938, 3942  /* 110 ~ 120°C */
};

/* ADC 原始值 → 温度×10（25.0°C → 250）。越界返回 -400 或 1200 */
static int16_t NTC_GetTemp(uint16_t adc)
{
    uint8_t min, max, k;
    uint16_t val;

    if (adc >= 4096) return -400;            /* 采样异常 */
    val = 4095 - adc;                        /* 反转：上拉接法，温度越高 ADC 越小 */
    if (val < NTC_Table[0])                  return -400;   /* 低于 -40°C */
    if (val > NTC_Table[160])                return 1200;   /* 高于 120°C */

    min = 0; max = 160;
    for (k = 0; k < 8; k++) {                /* 二分查找 */
        uint8_t mid = (min + max) >> 1;
        if (val <= NTC_Table[mid]) max = mid; else min = mid;
    }
    if (val == NTC_Table[min]) return (int16_t)min * 10;
    /* 线性插值 */
    {
        uint16_t lo = NTC_Table[min];
        uint16_t hi = NTC_Table[min + 1];
        uint16_t frac = (uint16_t)(((uint32_t)(val - lo) * 10) / (hi - lo));
        return (int16_t)min * 10 + (int16_t)frac;
    }
}
```

**显示格式**：温度×10 值 → `25.3`：整数部分 `t/10`，小数 `t%10`，用 `OLED_ShowNum`/`OLED_ShowString` 拼（或 `sprintf`）。
**异常**：返回 -400/1200 或采样超时 → 显示 `--`。

---

## 附录 B：补字脚本说明（步骤 2 用）

需要补的字：**仪、度、当、前**（12×12 字库缺这些字）。

```bash
python add_fonts.py 仪 度 当 前
```

- 脚本自动渲染 SimSun 点阵 → 生成字模 → 插入 `OLED_Fonts.c` 的 12×12 表
- 运行后检查输出"4 个字已插入"；重复运行会自动跳过已存在的字
- 需要安装 pillow：`pip install pillow`
- 改完后重新构建 + 烧录 + 验证页面文字
