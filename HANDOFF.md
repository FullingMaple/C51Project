# HANDOFF —— 2026-08-15 会话交接

> 用途：v0.3 OLED_UI 框架 C51 移植进度交接。新会话先读此文件，再读 `项目介绍.md` 与 `可行性评估报告.md`。
> 环境：Keil C51 9.60（D:/APP/C51V961）、STC8H8K64U 实验箱 9.62、0.96" SSD1306 OLED（硬件 I2C：SCL=P2.5、SDA=P2.4、0x78）、git 仓库（main + dev 分支，GitHub: FullingMaple/C51Project）。

---

## 1. 当前总体状态

**v0.3 里程碑（屏幕切换 OLED）✅ 完成：驱动 + 框架 + 平台层全部就绪，实体屏验证通过——静态帧率 80+，动画 30+，按键正常。**

| 模块 | 状态 |
|---|---|
| SSD1306 驱动（OLED_driver.c） | ✅ 实体屏点亮；硬件 I2C **400kHz**（I2CCFG=0xCD）；diff 逐页刷新；**总线自动恢复**（超时+GPIO 9 脉冲+重初始化） |
| Keil 工程（Project.uvproj） | ✅ 0 Error / 6 Warning（L13 递归静态检测×4 + L15 中断共享×2，均可接受），flash 61.6KB/64KB |
| 图形层 OLED.c | ✅ C51 兼容；**ShowImageArea 整列快路径 + ClearArea 页级掩码**（绘制 8 倍加速） |
| 字库 OLED_Fonts.c/.h | ✅ 裁剪未引用（Gif/LOGO/QR 等省 8.8KB）+ **补 29 字**（SimSun 点阵渲染，与原字库风格一致） |
| 框架核心 OLED_UI.c | ✅ 从上游重拷 + 39 处 C51 兼容化（块内声明提升、for 初始化区、C248、fmin/vsnprintf、patterns static code、HAL 删除、ShowFps 撞名改名） |
| OLED_UI.h | ✅ 从上游重拷修复（原文件被 python 写坏） |
| 菜单数据 OLED_UI_MenuData.c | ✅ 本项目菜单（磁贴 6 项：设置/关于/测温/串口/计算器/游戏）；List_MenuArea 已填实际区域；磁贴动画 UNLINEAR（无过冲） |
| 平台层 OLED_UI_Driver.c/.h | ✅ Timer0 20ms（12T）+ 16 键 ADC 键盘（P1.0 三态滤波）+ 空编码器 + 软延时 + GetTick + 总线恢复 |
| main.c | ✅ SP 不手动设（C51 运行库 0x22 已设）；OLED_UI_init/start + EA=1 |
| 帧率显示 | ✅ 默认开启（OLED_UI_FpsShow=true），设置页可关 |

## 2. 实测性能（用户上板确认，最终版 2026-08-15）

- **静态 80+fps、动画 35fps**（I2C 400kHz 整页 diff + 周期全刷 + 总线恢复，最终确认稳定）
- **按键正常**（上下/进入/返回），进入设置/关于页 ~1.5s（SPEED 8）
- **虚拟屏**（VIRTUAL_OLED=1）：显示正常（整屏发送 + 2 帧节流 + bUsbInBusy 忙跳过）
- **功能图标**：6 磁贴手绘图标（齿轮/温度计/DB9/计算器/手柄/关于）

## 2b. "按进入卡死"完整复盘（本次会话最重要的教训）

**症状演进**：400kHz 提速后按进入卡死 → 总线恢复修复（治标）→ 列级 diff 后复发 → 273kHz 稳定 → 400kHz 最终成功。

**根因**：400kHz 是 SSD1306 规格上限，实验箱**飞线条件下 I2C 时序余量不足，偶发事务失败一直存在**（R 计数诊断证实：静态下 I2C 就在持续超时，超时率 ~1/3）。"卡死"真相 = 每事务 2ms 超时 × 渐隐全刷大量事务 = 每帧几十毫秒 → 主循环被拖到 7-10 秒/帧。

**教训**：
1. **列级 diff（0x21 短事务）放大事务次数 → 失败频率超过容错能力**——整页 diff 事务少，400kHz 可稳定；列级 diff 必须与降速搭配
2. **容错 ≠ 根治**：总线恢复只是"失败能爬回来"，信号余量不足的病因要靠降速/硬件（SPI）解决
3. **诊断方法**：MainLoop D-A/B/C/D 标记定位卡死阶段 + **I2C 超时计数显示（R 值）确诊失败率**——不猜，用数据
4. **当前最优配置**：400kHz（0xCD）+ 整页 diff + 每 50 帧周期全刷 + 总线恢复（超时 2ms + 9 脉冲 + 重初始化）
5. **兜底方案**：若 400kHz 再出问题 → 273kHz（0xD4，MSSPEED=20）动画 30fps 稳定；或等电烙铁切 SPI（根治）

## 3. 本次会话踩坑（真金白银，新会话必读）

1. **OLED_UI.c 曾被写空、OLED_UI.h 被写坏**（python GBK 写失败）——已从上游重拷并重新兼容化（提交 7f14906）。**处理 GBK 文件必须 rb/wb + 同编码 + 统一 LF**；**不要用 Edit 工具改 GBK 文件**（会把中文注释写坏——OLED.c 因此坏过一次，git checkout 恢复后改用 python）
2. **I2CCFG 的 bit5-0 是 MSSPEED[5:0]（等待时钟数）**：速度 = FOSC/2/(MSSPEED×2+4)。**0xe0 在 24MHz 只有 176kHz**（官方 0xe0 是针对 11.0592MHz 的 100kHz 配置）！**400kHz 需 MSSPEED=13 → 0xCD**。参考：STC8H 硬件 IIC（CSDN）、STC 官方论坛
3. **I2C 总线偶发失败会"卡死"**：每个事务 13ms 超时（40000 次空循环）累积 → 画面冻结（诊断定位 D-D = OLED_Update）。**修复**：超时缩短 6000（~2ms）+ 连续失败 5 次触发 **I2C_BusRecover**（GPIO 9 脉冲释放从机 + 重新初始化 SSD1306，I2C_Recovering 防递归）——注意 I2C_Wait 调用 BusRecover 需要前向声明
4. **C51 声明位置**：块内任意位置声明报错；**连"赋值语句后声明"都报 C141**；for 初始化区声明不支持；结构体不能声明时用对象/函数返回值初始化（C248）
5. **未引用 const 数组不会被 REMOVEUNUSED 移除**——必须手动删（字库裁剪省 8.8KB 才装进 64KB）
6. **补字模**：SimSun 在 16px/12px 有内嵌点阵——大画布渲染 + bbox 居中 + **1:1 不缩放**（缩放有灰阶 → 笔画断/糊）。格式：**列式**（每列垂直 8bit 一字节，bit0 在顶；先全部列上半再全部列下半）
7. **ClearArea 页级掩码**：rows 必须限制在本页剩余行（min(8-j%8, y_end-j)），否则跨页清错位 → 界面残留（曾误判为按键失效）
8. **SP 不要手动设 0x80**：C51 运行库 ?C_C51STARTUP 已设 SP=0x22（栈 221B），手动 0x80 砍栈到 128B 深调用易溢出
9. **按键失效的排查规律**：改刷新/绘制路径（OLED.c/OLED_driver.c）后按键异常 → 先查 ClearArea/绘制函数的内存问题（越界/清错位），不要先怀疑按键链
10. **诊断方法**：MainLoop 加 D-A/B/C/D 标记定位卡死点（卡在 D-D = OLED_Update/I2C）；帧率判断用 P 值（刷新页数）区分 I2C/CPU 瓶颈
11. **LCD1602 打印调试**：无串口/无仿真器时可用 OLED 显示调试信息（FPS 位置 110,0）

## 3b. 本会话后半段新踩坑（虚拟屏 + 图标 + 卡死复盘）

1. **虚拟屏按进入卡死**：USB-CDC 库 `OLED12864_ShowPicture` 是**阻塞式发送**（等 EP 缓冲），渐隐每帧整屏 1024B、主机读取不及时 → 死等。修复：**整屏发送 + 每 2 帧节流 + bUsbInBusy 忙跳过**（忙则丢帧不阻塞）
2. **虚拟屏 OLED12864_ShowPicture 的 y/cy 参数语义未知**（库头无注释）——diff 页发送（`(0, page*8, 128, 1)`）导致显示错乱——**只信任整屏调用 (0,0,128,8)**
3. **Edit 工具会破坏 GBK 文件**（中文注释写坏，decode 失败）——**GBK 文件一律用 python rb/wb + 同编码处理**；main.c 被 Edit 改成了 UTF-8（编译无碍但 python 处理要用 UTF-8）
4. **printf 的 %d 读 uint8_t/uint16_t 显示错位**（0x40xx 恒定高位）——诊断计数显示用 uint16_t + extern 类型同步
5. **手绘图标**（PIL）：32×32 列式字模（先上半页再下半页）；齿轮用 8 径向梯形齿 + 细环壁（emoji 风格）；emoji 字符（🔧⚙️）不能出现在 GBK 文件/print 里（编码错误）
6. **SPEED 4→8**：页面切换动画从 3-5 秒降到 ~1.5s（UNLINEAR 收敛帧数减半）
7. **帧率显示默认开启**：OLED_UI_FpsShow 默认 true

## 4. 内存预算与功能规划（2026-08-16 评估 + 深度优化）

**当前占用（构建 2026-08-16 优化+修复后）**：Flash **47.0KB/64KB（剩 17.0KB）**、xdata 3861B/8KB（剩 4.2KB）、data 15.1B、0 Error/6 Warning

**优化成果（共回收 9.6KB）**：
1. **图标清理 -3.4KB**（提交 a1281b3）：9 个零引用图标（6×64×64 各 512B + Image_window/sleep 各 128B + Image_cube），C51 不移除未用 const。教训：新增图标前 grep 引用
2. **字库深度裁剪 -6.1KB**（本次）：按"代码字符串字面量 → 渲染尺寸分组"分析，只保留真实引用的字——
   - 16×16：99 → **13 字**（仅磁贴名：设置关于测温串口计算器游戏）
   - 12×12：122 → **33 字**（列表/窗口文本全集 31 汉字 + 开关符号 ■□）
   - 8×8：53 → **1 字占位**（FONT_8 无页面使用，留 1 字防链接错误）
3. **PRINTF_LARGE 试验**：改 PRINTF_SMALL 构建 code 完全不变（%.2f 由库实现，不依赖宏）→ **非优化点**，保持 LARGE

**⚠️ 裁剪引发两个显示 bug（已修复，上板确认过）**：
   - **"许可"被误删**：keep 集误写为"协议"（用 Read/文本工具读 GBK 文件会解码出错——实际菜单文本是"许可: Apache 2.0"！），导致裁剪删掉"许可"并白补"协/议"。**修复：从原表（提交 a1281b3）恢复"许可"+■□，删除多余"协议"**。教训：**字库分析必须字节级核对（python 读 bytes 找 0xD0ED 0xBFC9），工具显示可能骗人**
   - **关于页行文本超长触发滚动**：框架对超长行（>区域 120px）自动左滚（_LineSlip 循环），导致"开头被裁"假象。**修复：文案压缩至 ≤120px**（"-[项目] 电子手表式UI菜单"→" 电子手表式UI菜单"105px 等 4 行；" 许可: Apache 2.0"115px 合规）
   - **python 处理 GBK 的铁律（再强调）**：整文件重建必须 `data[:m.start()] + new + data[m.end()]` 替换式；`head+body+tail` 重建会把文件截断成只剩目标数组（已两次踩坑，一次 git checkout 找回）。改完必须验证文件完整性（assert 关键数组都在）

**⚠️ 后续加菜单文字的铁律**：新菜单/界面用到的汉字必须先查字库（`python 读 OLED_Fonts.c 的 Index`）——缺字要补（SimSun 12px/16px 点阵渲染，方法见 §3.6），否则显示空白（"协议"缺了多久没人发现）。字模成本：16×16=34B/字、12×12=26B/字。**每加一个磁贴/菜单项前先算字**。

**五功能评估（体积估算，未开工）**：

| 功能 | code+const | xdata | 补字 | 演示价值 | 批次 |
|---|---|---|---|---|---|
| NTC 测温 | 1.5~2.0KB | +0.1KB | ~110B（度/当前等） | ★★★ 手摸温度变 | 第一批 |
| 计算器 | 1.3~1.7KB | ~0 | 0（按钮全 ASCII） | ★★★ 键盘直观 + MDU16 亮点 | 第一批 |
| 倒计时闹钟（蜂鸣器 P5.4） | 1.3~1.8KB | ~0 | ~130B（分/秒/暂停） | ★★☆ 蜂鸣器响 | 第一批 |
| **时钟/万年历**（2026-08-16 新增） | ~1.5KB | ~0 | ~200B（年月日星期） | ★★★ 手表主题灵魂 | 第一批 |
| **红外遥控**（控菜单+按键音） | ~2.5KB | ~0 | ~130B（遥控/对码等） | ★★★ 遥控全场操作 | 第二批 |
| **贪吃蛇** | 1.5~2.2KB | +0.13KB | ~100B（开始/得分） | ★★★ 互动 | 第二批 |
| **2048**（2026-08-16 新增） | ~2.0KB | ~0.5KB（4×4 棋盘） | ~50B（得分） | ★★★ 互动 | 第二批 |
| 串口通信 | 1.5~2.5KB | +0.26KB（环形缓冲 256B） | ~160B（波特率/接收） | ★★☆ DB9 座直插（见下） | 第三批（超预算先砍） |
| **合计（全做 7 项）** | **~13.3~16.3KB** | +1KB | ~700B | | 预算内偏紧 |

**结论（2026-08-16 用户定案）**：7 项全做 ≈ 13.3~16.3KB（中值 ~14.8KB），17KB 预算内**可行但偏紧**（剩 ~1-3.7KB）。秒表/数码管已取消（用户决定）。**若超预算，砍序：串口 → 2048**。**开发顺序（Q8 定案）：红外遥控先行 → NTC→计算器→闹钟→时钟/万年历 → 贪吃蛇→2048 → 串口**；时间充裕（Q9），功能尽可能做好。每个功能完成后构建一次监控体积（目标：每批完成后剩 ≥2KB 余量）。

**📡 红外遥控设计（2026-08-16 定案，方案 A）**：
- **硬件**：接收头 U7 → P3.5（官方例程 29 背书）；T2 做 100µs 采样中断跑 Coody 解码（Timer0 已被 UI 占用）；NEC 协议，帧 108ms，响应上限 ~120ms
- **用户遥控器**：数字 0-9 + `*` `#` + ▲▼◀▶ + OK（共 16 键，红色 OK/*#、蓝色方向）
- **键位映射**：菜单 ▲▼=上下、OK/▶=确认、◀=返回；计算器 0-9=数字、`*`=×、`#`=÷、▲=+、▼=−、◀=C、OK==；游戏 ▲▼◀▶=方向、OK=暂停
- **对码**：EEPROM 存 5 键码（▲▼◀▶OK），带 magic+合理性校验；**无效自动进对码模式**（OLED 提示按各键，蜂鸣确认）；EEPROM 顺带存设置（亮度/深浅色/提示音）
- **方案 A（用户选定）**：**ADC 键盘代码保留作兜底**（~0.5KB 成本），`Key_Get*Status = 键盘 || 遥控`——遥控为主，键盘救场；开发期无遥控器也能调试
- **按键音联动**：所有按键（键盘+遥控）按下统一蜂鸣 ~50ms；设置页"提示音"占位项接上（P5.4，默认开）

**硬件可行性结论（原理图+官方例程核实，2026-08-16）**：
- ✅ 可行：NTC=P1.3/ADC3（例程18）、蜂鸣器=P5.4+BEEP1、红外=P3.5（例程29）、EEPROM 内置、**硬件 RTC（实验箱有 32.768K 晶振，例程 50）**
- ⚠️ 有条件：**DS18B20 数据线疑似与红外共用 P3.5（网络名 T1）——两者二选一，默认放弃 DS18B20**；LED 与数码管共用 P6（灯效与数码管互斥，二选一做数码管）
- ❌ 不可行：独立可调电位器（VREF 是基准非旋钮）、编码器（无）、触摸屏（存疑不冒险）
- ✅ 网络核实：UART2 波特率只能 T2（[官方论坛](https://www.stcaimcu.com/forum.php?mod=viewthread&tid=1671&extra=page%3D2&ordertype=2)）→ USB-CDC 方案必要；STC8H 普通 IO 可外部中断但 **C 版芯片仅电平模式**（[论坛](http://www.stcaimcu.com/forum.php?mod=viewthread&tid=1003&extra=&ordertype=2)）→ 红外维持 T2 采样法；USB-CDC 与虚拟 OLED 共用 CDC 通道（[冲突案例](https://www.stcaimcu.com/forum.php?mod=viewthread&tid=7242&extra=page%3D13&ordertype=1)已解决）

**🗳️ Grill 定案汇总（2026-08-16，用户逐项拍板）**：
| 决策点 | 定案 |
|---|---|
| Q1 时钟 | **硬件 RTC**（32K 晶振 + 例程 50；EEPROM 存时间戳恢复） |
| Q2 计算器 | **定点 2 位小数**（×100 整数运算，显示 3.50）；除零/溢出 Error |
| Q3 NTC 最大/最小 | **不加**（简洁状态栏） |
| Q4 游戏最高分 | **不存 EEPROM**（运行期记录） |
| Q5 串口断行 | **自动换行**（\n 分行 + 超宽折行） |
| Q6 遥控键码 | **硬编码**（开发期临时读码页读一次键码 → IR_KeyTable[] 写死；省对码 ~0.8KB；换遥控器改码重烧） |
| Q7 闹钟计时 | **Timer 秒计数自算**（RTC 只管走时显示） |
| 开发顺序 | **红外遥控先行**（Q8 定案），之后按批次推进：NTC→计算器→闹钟→时钟→贪吃蛇→2048→串口；时间充裕，功能尽可能做好（Q9） |

**⚠️ 串口引脚更正（2026-08-16 查原理图）**：实验箱 DB9 座经 **SP3232** 连的是 **UART2（P4.6=RxD2 / P4.7=TxD2）**；**P3.0/P3.1 被 USB 座占用（USB D-/D+）无排针引出**。**不要用 UART1 P3.0/P3.1**（要飞线且占 USB 下载脚）。

**📡 串口方案最终定案（2026-08-16，用户确认 USB-CDC）**：**不用 UART2/DB9**——UART2 波特率必须用 **T2**，与红外遥控的 T2 采样中断冲突。改为 **USB-CDC 虚拟串口**：实验箱 USB 座（P3.0/P3.1）直连 PC，串口助手显示为 COM 口收发，**零接线、零 T2 占用**。功能需求：PC 发送 → OLED 终端式滚动显示（不保存、不写 EEPROM）+ 状态行；可选 MCU→PC 回显。移植参考：官方 **61 号（CDC 协议）+ 70.2 号（串口绘图 USB-CDC）**。⚠️ 开发期 VIRTUAL_OLED=1 与虚拟 OLED 可能争用 USB（需实测）；串口按实体屏模式设计。

**字库现状（补字成本参考）**：16×16 共 99 字（34B/字）、12×12 共 122 字（26B/字）、8×8 共 53 字（10B/字）。磁贴名"测温/串口/计算器/游戏"所需字已齐（测+温、串+口）。新页面标签汉字按上表补字。

## 4b. ⚠️ "按进入卡死"复发诊断（2026-08-16，暂搁置）

**现象**：按进入键 → 画面冻结、按键无反应、不自动恢复（1 分钟）；蜂鸣器心跳正常、FPS 数字在变（中断+主循环+I2C 都活着）→ **非死锁，是框架动画/渲染状态卡住**

**诊断结论（已排除）**：
- ❌ 遥控相关：T1 中断禁用（ET1=0）后照卡 → 与 IR 无关
- ❌ 本次改动：**v0.3 基线（543669b）照卡** → 与蜂鸣器/强制全刷/Key 合并无关
- → **硬件/环境问题**：8-15 验证正常、8-16 同代码卡死——飞线（P2.5/P2.4）接触、供电、环境干扰变化

**决定（用户 2026-08-16）**：暂搁置，**明天电烙铁到位后改 SPI 根治**（见项目介绍 §2.2：R173/R174 出厂 SPI + J11 7 孔座，无需飞线）

**SPI 根治依据**：I2C 400kHz 是 SSD1306 规格上限（飞线余量不足 → 偶发事务失败 → 2ms 超时 × 渐隐动画事务风暴 → 卡顿）；SPI 全屏 <1ms（比 I2C 快 20 倍以上）且无 I2C 总线锁死故障模式 → 事务量不再构成瓶颈

**✅ SPI 后端已完成（2026-08-16，双模式构建验证通过）**：
- config.h：`OLED_IF_SPI` 宏（1=SPI / 0=I2C 可切换）+ 引脚模式分支
- OLED_driver.c：SPI 后端（官方 69 号例程 SPI 版移植）——SPI1 硬件 6MHz（4T）+ 模式 0 + MSB 先
  - 引脚：SCLK=P2.5（SPI1 组1）MOSI=P2.3 RES=P2.4 DC=P3.4 CS=P1.1（后 3 根 GPIO）
  - I2C 辅助函数 `#if !OLED_IF_SPI` 包裹（SPI 下自动裁剪）；初始化序列/强制全刷/diff/周期全刷共用
  - 构建：SPI 0 Error/2 Warning（code 41771），I2C 0 Error/8 Warning（42013）无回归
- **接线（模块到货后）**：SSD1315 7 针模块直插 J11 7 孔座 SPI 位
  （1=GND 2=VCC 3=SCLK 4=D1(MOSI) 5=RES 6=DC 7=CS），实验箱出厂 R173/R174（SPI 档）已焊，零焊接
- **注意**：SSD1315 与 SSD1306 指令集兼容（初始化序列共用）；若显示异常优先查方向（0xA1/0xC8）与 RES 时序（模块 RES 脚需要上电复位脉冲，驱动已含 0xAE→0xAF 序列，若首次不亮检查 RST 引脚电平）
- 之后恢复 dev 最新（ed09256）继续功能开发（遥控已集成完成）

## 5. 剩余事项（可选）

1. 动画帧率 30fps 已到 SSD1306 规格上限（400kHz）——若想更高：减少动画期全屏刷新（区域刷新）或接受现状
2. 6 个 Warning 可接受（L13 递归为静态检测，运行时有 I2C_Recovering 重入保护；L15 中断共享为框架架构）
3. 设置页"语言/提示音"占位项未实现功能（回调为 NULL）
4. "测温/串口/计算器/游戏"磁贴无子菜单（按进入无反应是预期行为）——功能开发按 §4 批次推进
5. 字库仍是子集——菜单新增汉字需补字模（见 §3.6 方法）
6. 功能开发完成后每个构建检查体积（Flash 预算 11.1KB，见 §4）

## 5. 构建与验证命令

```bash
# 构建（命令行，无需打开 Keil）
"D:/APP/C51V961/UV4/UV4.exe" -b "Project.uvproj" -o "build_log.txt" && sleep 25 && grep -E "error|Program Size" build_log.txt
# 产物：Objects/Project.hex（STC-ISP 烧录，IRC 24MHz）

# git（dev 分支）
git add -A && git commit -m "..." && git push origin dev
```

## 6. 参考资料位置

- 上游框架镜像：`D:/Document/本科/单片机课程/OLED_UI_upstream/`（OLED_UI_Core/HAL/OLED_UI_Core/）
- 官方例程：`D:/edge_download/STC8H8K64U-DEMO-CODE-V9.6/`（17=ADC键盘、28=I2C主机、69=I2C OLED 双后端）
- 实验箱原理图：`课程资料/单片机/STC8H8K64U-DEMO-CODE-V9.6/实验箱9.62_2023-4-25-SCH.pdf`
- STC8H 手册（I2CCFG 定义）：https://www.stcmicro.com/datasheet/STC8C-cn.pdf ；STC 官方论坛 https://www.stcaimcu.com
- 项目文档：`项目介绍.md`（v0.3）、`可行性评估报告.md`（OLED v3）
