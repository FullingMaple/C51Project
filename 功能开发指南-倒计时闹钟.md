# 倒计时闹钟功能开发指南（AI 协作版）

> 前置：先读 `功能开发指南-计算器.md` 的 §0（工作方式 + 分支铁律 + Skill 用法）和 §2（环境速通——虚拟屏/构建/烧录），本指南只讲闹钟本身。若先做秒表，可先读 `功能开发指南-秒表.md`（全局计时驱动的做法相同）。

## 1. 任务总览

| 项 | 内容 |
|---|---|
| 功能 | 主界面"闹钟"磁贴（第 4 位）→ 闹钟页：设定时长 → 倒计时 → 归零蜂鸣提醒 |
| 输入 | 物理键 1/2/3/4 + 遥控 ▲▼/OK/◀（映射一致） |
| 显示 | 虚拟 OLED 屏（VIRTUAL_OLED=1） |
| 验收 | §5 验证清单（10 项全过） |

**你只允许改动 3 个文件**（其余一律不许动）：

| 文件 | 改动 | 编码 |
|---|---|---|
| `src/ui/OLED_UI/OLED_UI_MenuData.c` | 闹钟页（页面/绘制/按键/状态机） | **GBK** |
| `src/ui/Driver/Hardware_Driver/OLED_UI_Driver.c` | 全局计时驱动（Alarm_Tick 倒计时 + 蜂鸣循环状态机） | **UTF-8** |
| `src/ui/Driver/Software_Driver/OLED_Fonts.c` | 补字模（用 add_fonts.py，不要手改） | **GBK** |

> ⚠️ **文件编码不同**：MenuData.c / OLED_Fonts.c 是 GBK，OLED_UI_Driver.c 是 UTF-8——AI 改哪个文件用哪个编码，改完验证中文没乱码。**GBK 文件禁止用文本编辑工具直接改**（用 python rb/wb 或 add_fonts.py）。

## 2. 验收标准（先看目标长什么样）

**设定模式**（进入即设定）：
```
┌────────────────────────────┐
│      [05]:30:00            │  ← 8×16 大字号，反色框指示当前焦点
│                            │
│    [开始]  返回            │  ← 底部二选一（反色框指示）
└────────────────────────────┘
```

**计时中**：
```
┌────────────────────────────┐
│       05:29:47             │  ← 大数字倒计时
│                            │
│   1暂停 2重置 3复原        │  ← 底部提示（6×8）
└────────────────────────────┘
```

**时间到（响铃）**：
```
┌────────────────────────────┐
│       时间到！             │  ← 12×12 大字提示
│                            │
│   按任意键停止蜂鸣         │
└────────────────────────────┘
```

**交互**（状态机：设定 → 计时中 → 暂停 → 响铃）：
- **设定模式**（进入即此态）：焦点序列 **时 → 分 → 秒 → 选项（开始/返回）**
  - 焦点在字段（反色框在数字上）：键 1/2 调值（循环回绕：时 0-99、分/秒 0-59）、键 3 推进到下一焦点
  - 焦点在选项（反色框在"开始/返回"上）：键 1/2 切换选项、键 3 执行（开始→计时；返回→退出页面）
  - 键 4 任何时刻退出页面（放弃设定）
- **计时中**：键 1 = 暂停/继续、键 2 = 重置（回设定时长重新开始）、键 3 = 复原（回设定模式，保留设定值）、键 4 = 退出（**计时继续，全局状态**）
- **暂停中**：键 1 = 继续；键 2/3/4 同计时中
- **响铃（时间到）**：蜂鸣 **3 次短鸣（~1 秒）→ 停鸣（~1 秒）→ 循环** + 界面显示"时间到"；**任意键停止蜂鸣**（按键只停闹，不触发菜单操作）→ 回设定模式（保留上次设定值）
- **任意状态退出页面 → 再进入**：计时继续（全局状态）、设定值保留；断电清零（不保存）
- **响铃时在任何页面（主菜单/设置页等）蜂鸣都响**（全局驱动）

## 3. 任务书（复制给 AI）

```
# === 任务书开始 ===

你是 STC8H8K64U 单片机的 C51 嵌入式工程师。项目是"电子手表风格 UI 菜单系统"
（OLED_UI C51 移植框架，Keil C51，显示走虚拟 OLED）。
你的任务：实现倒计时闹钟页面。所有代码 C51 兼容（C89 子集）。

## 背景（必须先了解）
- 框架已提供：Key_GetRawKey()（键码 1~16，0=无键；含遥控逻辑键）；
  OLED_Printf/OLED_ShowString/OLED_ShowMixString/OLED_ReverseArea/OLED_Clear；
  OLED_8X16_HALF、OLED_6X8_HALF、OLED_12X12_FULL、OLED_7X12_HALF
- 页面模型：0 菜单项页 + 每帧回调 General_ShowAuxiliaryFunction
- 范例：src/ui/OLED_UI/OLED_UI_MenuData.c 的 TimeAuxFunc（时间页）——
  0 菜单项页面怎么建、怎么在 AuxFunc 里读键/绘制/反色，照它写
- Timer0 20ms 中断在 OLED_UI_Driver.c（Driver_TickHandler 每 20ms 调一次）

## 任务 1：全局驱动（OLED_UI_Driver.c）
闹钟必须全局计时 + 全局蜂鸣（退出页面继续跑，归零任何页面都响）。
新增（必须显式初始化 =0，工程无 STARTUP.A51）：
  uint8_t  AL_State;    /* 0=设定 1=计时中 2=暂停 3=响铃 */
  uint8_t  AL_SetH, AL_SetM, AL_SetS;   /* 设定值（时/分/秒） */
  uint32_t AL_Remain;   /* 剩余 0.1s 计数（计时精度 0.1s，显示取整到秒） */
  uint8_t  AL_BeepPhase;/* 蜂鸣循环状态机：0=鸣 1=停 */
  uint8_t  AL_BeepCnt;  /* 当前相位剩余 tick 数 */
- 在 Timer0 ISR（Driver_TickHandler 同处）每 20ms 驱动：
  a. 倒计时：if(AL_State==1){ 每 5 tick 减 1（0.1s）；减到 0 → AL_State=3（响铃）}
  b. 蜂鸣循环（AL_State==3）：相位机——
     鸣相位：蜂鸣 3 次短鸣（每次 60ms 间隔 60ms，约 1 秒）→ 切停相位
     停相位：停 1 秒 → 切回鸣相位（循环）
     实现：用 AL_BeepPhase/AL_BeepCnt 计数，Buzzer 引脚操作参照现有
     Buzzer_Beep/Buzzer_Tick 机制（P5.4 低电平响；长鸣用直接置位+计时恢复）
  c. 响铃按键消费：响铃中任意键按下 → AL_State=0（回设定）、停蜂鸣、
     清除该次按键（不让它触发菜单操作）——在 OLED_UI_InterruptHandler
     按键处理之前判断（或框架层配合，见任务 3 注）
- 头文件 OLED_UI_Driver.h 加 extern 声明

## 任务 2：闹钟页（MenuData.c）
1. 磁贴：MainMenuItems 插入 {"闹钟", NULL, &AlarmMenuPage, NULL, NULL, NULL,
   Image_alarm, NULL, 0, 0}（第 4 位，秒表之后；Image_alarm 图标见任务 4）
2. 页面：MenuPage AlarmMenuPage = { MENU_TYPE_LIST, SPEED, NOT_SHOW, UNLINEAR,
   OLED_UI_FONT_12, &MainMenuPage, AlarmMenuItems(0 项), 0, AlarmAuxFunc, {0,0,128,64}, ... }
3. AlarmAuxFunc 每帧：
   a. 读键：k = Key_GetRawKey(); 边沿去重（static 上次值）
   b. 状态机（AL_State）：
      - 0 设定模式：static 焦点 Focus（0=时 1=分 2=秒 3=选项）+
        static 选项 Choice（0=开始 1=返回）
        Focus 0~2：键 1/2 调 AL_SetH/M/S（循环回绕：时 0-99、分/秒 0-59）、
                  键 3 → Focus++（到 3）
        Focus 3：键 1/2 切 Choice、键 3 执行（Choice==0 → 计算 AL_Remain
        = (H*3600+M*60+S)*10，AL_State=1；Choice==1 → OLED_UI_Back 退出页面）
        键 4 → OLED_UI_Back 退出
      - 1 计时中：键 1 → AL_State=2（暂停）；键 2 → AL_Remain 重置为设定值
        （重新开始）；键 3 → AL_State=0（回设定，保留 AL_Set*）；键 4 → 退出
      - 2 暂停中：键 1 → AL_State=1（继续）；键 2/3/4 同计时中
      - 3 响铃：本页不处理按键（全局驱动消费，见任务 1c）
   c. 绘制（按 AL_State）：
      - 0 设定：OLED_Printf(居中, 8, OLED_8X16_HALF, "%02d:%02d:%02d",
        (int)AL_SetH, (int)AL_SetM, (int)AL_SetS)；焦点反色框：
        Focus==0 → ReverseArea(时字段位置)；==1 → 分；==2 → 秒；
        底部"开始  返回"（OLED_ShowMixString 12×12 汉字 + 6×8 空格），
        Choice 反色框（24px 盖两字，参照时间页二选一）
      - 1/2 计时/暂停：大数字显示 AL_Remain/10 的时:分:秒；底部提示
        "1暂停 2重置 3复原"（暂停时"1继续 2重置 3复原"）
      - 3 响铃：12×12"时间到"居中 + 6×8"按任意键停止蜂鸣"提示
      - 注：%02d 的 uint8_t 参数必须 (int) 强转（C51 变参粘连坑）
4. 页内不要动 CurrentMenuPage；退出页面用 OLED_UI_Back()（设定模式）
   或框架 Back（键 4 松开沿自动触发）

## 任务 3（框架配合，需主开发确认）：响铃按键消费
响铃时任意键"只停闹不触菜单"——最好在 OLED_UI_InterruptHandler 开头加：
  if(AL_State == 3){ 若检测到任意键（Key_GetRawKey()!=0 或按键沿）→
  AL_State=0; 停蜂鸣; return; }（跳过本帧全部按键处理）
——若主开发已实现此钩子，页内无需处理；否则在驱动层实现。

## 任务 4：补字模 + 图标（用脚本，勿手改）
- 12×12 补字：python add_fonts.py 闹 钟 开 始 返 回 重 置 复 原 暂 停
  继 续 时 间 到 翻 页（按最终提示文案核对用字，缺什么补什么）
- 16×16 补"闹 钟"（磁贴名）：add_fonts16_icon.py 或主开发补
- 图标：Image_alarm（32×32 闹钟样式：圆钟身+双铃+锤，参考 Image_clock 画法）
  加到 OLED_Fonts.c + OLED_Fonts.h extern

## C51 铁律（违反会编译失败/显示错乱）
1. 变量声明必须在函数/块开头（C141）；for 初始化区不能声明变量
2. 变参 printf（OLED_Printf/PrintfMix）里 uint8_t 参数必须 (int) 强转
3. 所有全局/static 变量必须显式初始化 =0（无 STARTUP.A51）
4. GBK 文件（MenuData.c/OLED_Fonts.c）禁止文本编辑器直接改——python rb/wb 同编码
5. 页面切换动画/渐隐由框架处理，页面内不要动 CurrentMenuPage
6. 蜂鸣循环是全局状态机（ISR 驱动），页面只是显示——别把蜂鸣逻辑放页面回调里

# === 任务书结束 ===
```

## 4. 补字与资源预算

| 项 | 内容 |
|---|---|
| 12×12 补字 | 闹 钟 开 始 返 回 重 置 复 原 暂 停 继 续 时 间 到（~15 字 × 26B ≈ 390B，按最终文案核对） |
| 16×16 补字 | 闹 钟（2 字 × 34B = 68B，磁贴名） |
| 图标 | Image_alarm 32×32（128B） |
| 代码/数据 | ~1.0-1.5KB |
| **合计** | **~1.5-2.0KB**（当前预算剩 18.4KB，充足） |

> 补字前先查字库：`python -c "读 OLED_Fonts.c 的 Index 比对"`——缺字才补。注意"时间到"的"间/时"已有、"到"需补。

## 5. 验证清单（全过才算完成）

- [ ] 1. 主菜单出现"闹钟"磁贴（第 4 位），图标正常
- [ ] 2. 进入闹钟页：显示 00:00:00，反色框在"时"字段，底部"开始 返回"
- [ ] 3. 设定：键 3 推进焦点（时→分→秒→选项），键 1/2 调值（回绕：时 0-99、分/秒 0-59）
- [ ] 4. 选项焦点选"开始"键 3 → 开始倒计时（大数字走时）；选"返回"→ 退出页面
- [ ] 5. 计时中键 1 暂停/继续、键 2 重置（回设定时长）、键 3 复原（回设定模式保留值）
- [ ] 6. **归零**：蜂鸣 3 短鸣→停 1s→循环 + 显示"时间到"
- [ ] 7. 响铃时在主菜单等**其他页面**蜂鸣也响；按任意键停止（不触发菜单操作）→ 回设定模式
- [ ] 8. **退出页面再进入**：计时中退出 → 再进计时继续；设定值保留
- [ ] 9. 遥控 ▲▼/OK/◀ 操作与物理键一致；按键音正常
- [ ] 10. 构建 0 Error / 不超过 2 Warning；内存 Flash 总量 < 50KB；提交后通知主开发合入验证实体屏
