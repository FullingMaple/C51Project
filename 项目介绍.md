# 电子手表风格 UI 菜单系统 —— 项目介绍

> 基于 STC8H8K64U 实验箱 9.62 与 0.96" SSD1306 OLED（128×64）液晶屏的综合实训项目
> 文档版本：v0.3（2026-08-14：屏幕由 LCD12864 切换为 0.96" OLED，实体屏已点亮验证，虚拟 OLED 调试路径就绪；v0.2 为 ST7920 版历史记录）

---

## 1. 项目概述

本项目以 STC8H8K64U 单片机实验箱为平台，利用 **0.96 寸 SSD1306 OLED 屏（128×64，软件 I2C）**，构建一套**电子手表风格**的流畅 UI 菜单系统。系统通过实验箱的 16 键 ADC 键盘操作（选择、进入、返回），以"磁贴桌面 + 多级列表菜单"的形式整合多项实用功能。



### 项目亮点

- **流畅的 UI 交互**：framebuffer + **diff 局部刷新引擎**——SSD1306 软件 I2C 全屏刷新 ~10~24ms（40~106fps，无忙检测），diff 引擎只刷新变化行，菜单高亮移动 <0.5ms、静态界面零开销
- **动画全面解锁**：ST7920 时代被 12fps 上限卡死的**全屏滑动/淡入动画现在全部可达 40fps+**，游戏实现流畅
- **完整的多组件 UI**：磁贴桌面（首屏）、多级列表菜单、窗口组件、滑动/淡入动画、GIF 图标动画
- **深度定制的中文界面**：内置 GB2312 12×12 中文字模（12×12/16×16 双档），菜单全中文
- **模块化分层架构**：驱动层 / UI 框架层 / 应用页三层解耦，新增功能 = 新增一个菜单页
- **设置掉电保存**：内置 EEPROM 存储用户设置，开机自动恢复
- **亮度真实可调**：SSD1306 0x81 对比度寄存器（OLED 自发光，无背光限制）
- **性能仪表盘**：内置帧率显示开关，实时展示系统刷新率

---

## 2. 硬件平台

### 2.1 主控芯片：STC8H8K64U

- 8051 内核（1T 增强型），主频 24MHz
- 64KB Flash 程序存储，8KB SRAM（xdata），256B IRAM
- 内置 EEPROM（IAP 方式）、12 位 ADC（15 通道）、USB 2.0-FS
- 3 个定时器、UART×4、I2C/SPI 等丰富外设
- **MDU16 硬件数学单元**：乘除/开方硬件加速（计算器可选使用）

### 2.2 使用的实验箱外设

| 外设 | 接线/通道 | 用途 |
|---|---|---|
| 0.96" SSD1306 OLED（128×64） | 软件 I2C：SCL=P2.5，SDA=P2.4，地址 0x78；P2.2~P2.5 开漏+外部上拉 | 主显示 |
| 16 键 ADC 键盘 | P1.0 / ADC 通道 0（电阻分压式） | 唯一输入设备 |
| NTC 热敏电阻 | P1.3 / ADC 通道 3（MF52 10K@25°C，B=3950） | 测温功能（v1） |
| 蜂鸣器（有源） | P5.4 | 按键提示音 |
| UART1 | P3.0/P3.1（默认映射） | 串口通信（v1） |
| USB | CDC/HID 免驱 | 程序下载（与串口通信互不冲突） |

> 屏幕切换说明（2026-08-14）：原方案为实验箱自带 LCD12864（ST7920，8 位并口，全屏刷新 12fps 上限），改用 0.96" SSD1306 OLED 后全屏刷新 40~106fps，动画/游戏/亮度调节全部升级为真功能。仅占用 P2.4/P2.5 两根 IO（原方案占用 P6 数据口 + 5 根控制线）。详见[可行性评估报告.md](可行性评估报告.md)。

**实体屏接线（2026-08-14 已验证点亮）**：
- 实验箱 OLED 接口区域只有 7 孔座（J11），J10 4 针座出厂未焊
- 7 孔座 I2C 孔位：1=GND、2=VCC(3.3V)、3=SCL(P2.5)、4=SDA(P2.4)、5=RES、6=DC、7=CS
- **模式电阻**：出厂焊 R173/R174（SPI），I2C 需改焊 R175/R176（或短接其焊盘）；4 针模块按 GND-VCC-SCL-SDA 直插 1~4 孔，或飞线（SCL/SDA 从 R175/R176 空焊盘取）
- 显示方向：69 号例程初始化 `0xC0` 上下翻转 → 改 `0xC8`；左右镜像改 `0xA0↔0xA1`

---

## 3. 功能详述

### 3.1 交互设计通则（全局操作规范）

所有界面遵循同一套按键逻辑（16 键 ADC 键盘，当前占用 4 键，其余预留）：

| 操作 | 键 | 行为 |
|---|---|---|
| 上 / 下 | 占位键 1/2（待按箱子布局确认） | 菜单项/磁贴移动；**长按 0.8s 后连发**（10 键/秒），支持快速滚动长列表 |
| 确认（进入） | 占位键 3 | 进入子菜单 / 触发菜单项回调 / 切换开关项 |
| 返回 | 占位键 4 | 返回上级菜单；设置页返回时触发"统一保存" |

- 每次按键有**提示音**（P5.4 蜂鸣器，占位：短滴），可关闭
- 页面切换带**淡出动画**（FadeOut 遮罩，5 级棋盘格渐变）与**列表滑动动画**（PID 曲线插值）
- 深浅色主题影响全局（硬件反色），不改变布局

### 3.2 主界面（磁贴桌面，开机首屏）✅ v0

```
┌────────────────────────────────┐
│  [设置]  [关于]  [测温]        │
│  [串口]  [计算器][游戏]        │
└────────────────────────────────┘
    4×2 磁贴宫格（32×32px 图标）+ 圆角选择框动画
    底部滚动条 + 页内指示箭头
```

- 6 个功能磁贴：设置 / 关于 / 测温 / 串口 / 计算器 / 游戏，每个带独立图标（Image_* 字模，code 段）
- **选择框动画**：圆角矩形选择框在磁贴间移动，带位置/尺寸的 PID 插值（配置在框架 config）
- 磁贴焦点移动 = 局部刷新（<0.5ms/帧），滑动动画期间仅动画区域刷新
- 磁贴图标缺失时自动显示 UnKnown 占位图标（框架内置容错）
- 注：0.96" 屏（26.9×13.4mm）上 32×32 磁贴占半屏，布局已按小屏优化

### 3.3 设置 Settings ✅ v0（EEPROM 持久化 v0.3 补）

设置页为纵向列表，每项右侧显示开关状态（`*` 前缀 = 开，`-` = 关）。**改动只更新内存变量，退出设置页时统一写 EEPROM 一次**（避开热路径；擦除扇区 1~5ms 暂停发生在按键间隙，无感）。

| 设置项 | 行为 | 状态 |
|---|---|---|
| 亮度 | 弹出**窗口组件**（圆角矩形窗口 + 数据条），按上下键调 `OLED_UI_Brightness`（0~255，步进 5），写 SSD1306 **0x81 对比度寄存器**，真实可调 | ✅ v0.3（OLED 自发光，无背光硬接限制） |
| 深浅色模式 | 切换 `ColorMode` → SSD1306 指令 **0xA7（反色）↔ 0xA6（正常）**，整屏即时反色，framebuffer 内容不变 | ✅ 已实现 |
| 显示帧率 | 切换 `OLED_UI_FpsShow` → 屏幕右上角以 6×8 小字实时显示当前刷新率（每 1s 更新统计） | ✅ 已实现 |
| 语言（占位） | 切换 `LangCN`（中文/英文），框架已具备双语字模（12×12 中文 + 7×12 ASCII），逻辑待接 | ⏳ 占位 |
| 提示音（占位） | 切换 `SoundOn`（蜂鸣器开/关），P5.4 驱动代码待接 | ⏳ 占位 |
| [返回] | 触发保存流程 + 返回主界面 | ✅ |

### 3.4 关于 About ✅ v0

静态信息列表（只读）：项目名称、主控 MCU（STC8H8K64U @24MHz）、屏幕（0.96" SSD1306 128×64 OLED）、UI 框架版本（OLED_UI 0.2.1）与技术来源（Apache 2.0 署名）。条目为普通列表项，无回调。

### 3.5 NTC 测温 ⏳ v1

| 项目 | 方案 |
|---|---|
| 数据来源 | P1.3 / ADC 通道 3（MF52 10K@25°C，B=3950），12 位 ADC |
| 温度换算 | 161 点温度-ADC 查表 + 二分查找 + 线性插值（复用实验十四 `ntc.c`，整数×10 定点，避免浮点 printf） |
| 界面布局 | 上半屏大字号温度值（16×16 中文字模"温度" + 8×16 数字），下半屏状态栏（最大值/最小值） |
| 刷新策略 | 1s 采样一次，只刷新数字区域（1-2 行局部刷新，<0.5ms） |
| 量程 | -40 ~ +120°C |

### 3.6 串口通信 ⏳ v1

- **物理层**：UART1 @ 115200 8N1（P3.0/P3.1），接收中断 + 256B 环形缓冲；下载走 USB，互不冲突
- **协议草案**（行协议，`\n` 结尾）：
  - 普通文本行 → 直接显示在串口页屏幕（终端式，区域滚动）
  - `@` 开头的行 → 命令模式：`@page <n>` 切换页面、`@temp` 查询温度等（命令表 v1 细化）
- **界面**：串口页显示最近接收内容 + 状态行（波特率/接收计数）

### 3.7 数字计算器 ⏳ v2

- **键盘映射**（16 键 ADC 键盘恰好完整覆盖，计算器页直读原始键值）：

| 1 | 2 | 3 | + |
|---|---|---|---|
| 4 | 5 | 6 | − |
| 7 | 8 | 9 | × |
| 0 | C | = | ÷ |

- **界面**：上方表达式行（当前输入 + 运算符），下方结果行；整数/定点运算（避免浮点库）
- 支持：连续运算、清屏、错误提示（除零/溢出显示"Error"）
- 可选用 MDU16 硬件乘除加速

### 3.8 游戏 ⏳ v2/v3

| 游戏 | 玩法 | 界面方案 | 性能方案 |
|---|---|---|---|
| 贪吃蛇 | 方向键控制，吃豆增长，撞墙/咬尾结束，速度随长度提升 | 全屏 16×8 网格（8px 格） | 每帧只刷蛇头/蛇尾 2 格（<0.5ms） |
| 俄罗斯方块 | 经典玩法：7 种方块、旋转、加速下落、消行计分 | 缩小场地：4px 格 10×16 或 6px 格 10×10 + 右侧分数/下一个方块 | 每次只刷下落行与消行（<1ms）；全屏重绘亦可 |
| 小恐龙 | 跳跃/蹲下躲避障碍，距离计分 | 地面滚动带 + 恐龙/障碍点阵 | 地面带按列局部刷新；OLED 全屏能力富余，帧渲染无压力 |

- 游戏通用：`[返回]` 退出回游戏菜单；暂停功能；游戏内使用框架局部刷新保证流畅
- 游戏入口：磁贴"游戏"→ 游戏列表子菜单 → 选择游戏启动

### 3.9 实时时钟 💤 预留

- 时间源接口预留：v0 不做时钟主屏；后续可接 DS3231 RTC 模块（I2C）或软件时钟（Timer 累计）
- 主界面磁贴预留位置，不影响现有布局

### 3.10 功能状态总览

| 功能 | 状态 | 说明 |
|---|---|---|
| 磁贴主界面 | ✅ v0 | 6 磁贴 + 选择框动画 |
| 多级列表菜单 | ✅ v0 | 上下/确认/返回 + 长按连发 |
| 设置（主题/帧率/亮度窗口） | 🔧 v0.3 | 亮度改为 OLED 0x81 真实调节；EEPROM 持久化 v0.3 补 |
| 关于 | ✅ v0 | 信息展示 |
| NTC 测温 | ⏳ v1 | 代码已具备（实验十四），待接入 |
| 串口通信 | ⏳ v1 | 协议草案已定 |
| 计算器 | ⏳ v2 | 键盘映射已规划 |
| 贪吃蛇 / 俄罗斯方块 / 小恐龙 | ⏳ v2/v3 | 界面与性能方案已定 |
| 实时时钟 | 💤 预留 | 接口预留 |

> ✅ 已完成　🔧 进行中　⏳ 已规划　💤 预留

---

## 4. 系统架构

### 4.1 分层结构（OLED_UI 移植版）

```
┌─────────────────────────────────────────────────┐
│ 应用页层（菜单数据 + 回调）                        │
│  OLED_UI_MenuData.c —— 磁贴主屏 + 设置/关于/占位页  │
├─────────────────────────────────────────────────┤
│ UI 框架层（上游 OLED_UI 核心，C51 移植）           │
│  OLED_UI.c —— 菜单树/动画/窗口/按键状态机          │
│  OLED_UI_Launcher.c —— 启动封装                  │
│  OLED.c —— 图形绘制层（framebuffer 操作）          │
│  OLED_Fonts.c —— 中英文字模 + 图标（code 段）      │
├─────────────────────────────────────────────────┤
│ 平台适配层（STC8H8K64U 版，重写）                  │
│  OLED_driver.c —— SSD1306 软件 I2C 驱动 + diff    │
│  OLED_UI_Driver.c —— 16键ADC键盘 + Timer0 20ms    │
├─────────────────────────────────────────────────┤
│ 底层：STC8H 头文件 + stdint/stdbool shim          │
└─────────────────────────────────────────────────┘
```

### 4.2 运行模型（事件驱动 + 定时节拍）

```
Timer0 20ms 中断 ──► OLED_UI_InterruptHandler（按键记录/动画推进/帧率计数）
                              │
主循环 OLED_UI_MainLoop：
  按键状态机（上/下长按连发、确认/返回边缘检测）
      │
  动画插值（PID 曲线 / 非线性移动）
      │
  渲染到 framebuffer（OLED_DisplayBuf，1KB xdata）
      │
  OLED_Update() —— diff 对比影子缓冲，只刷新变化区域 → SSD1306
```

- **不在中断里刷屏**：I2C 传输在中断外执行，主循环独占显示总线
- 上下键长按连发由框架内置（PRESS_TIME 0.8s → 连发），适配菜单快速滚动

### 4.3 SSD1306 驱动与 diff 刷新引擎

- **SSD1306 无忙检测**：写入为 I2C 流式传输（软件 I2C 400kHz~1MHz），全屏 1024 字节 ≈10~24ms（40~106fps），远快于原 ST7920 的 83ms/12fps
- **diff 引擎**：驱动内维护 1KB 影子缓冲，`OLED_Update()` 逐行比对显存，只把变化的区域写入 OLED：
  - 菜单高亮移动（2 行）：<0.5ms
  - 光标闪烁/数字变化（1 行）：~0.2ms
  - 动画帧（滑动区域）：仅动画区域刷新
  - 静态界面：零开销
- **双后端驱动**（`#if VIRTUAL_OLED` 编译开关）：
  - 虚拟后端：经 USB-CDC 调 `OLED12864_ShowPicture` 整屏发到 AiCube-ISP 虚拟 OLED（`工具→调试仿真接口→OLED-128*64`），开发期无实体屏调试全 UI
  - 实体后端：软件 I2C（SCL=P2.5、SDA=P2.4，P2M1/P2M0=0x3c 开漏 + 10K 上拉 3.3V，地址 0x78）
- 初始化序列（实测点亮）：0xA1 段重映射（文字镜像改 0xA0）、0xDA 0x12 COM 配置（半边亮改 0x02）、0x8D 0x14 电荷泵、0x81 对比度；**上下颠倒改 0xC0↔0xC8**（69 号例程用 0xC0 需改 0xC8）
- 深浅色主题：0xA7（反色）↔ 0xA6（正常）

---

## 5. 移植记录（上游 OLED_UI → C51/STC8H）

> 2026-08-14 屏幕切换：驱动层目标由 ST7920 改为 SSD1306，其余各列不受影响。

### 5.1 移植范围

| 层 | 处理 | 工作量 |
|---|---|---|
| UI 核心 / 图形层 / 字库 | **C51 兼容性修复**：~70 处（for 内声明、块内声明必须在语句前、designated initializers 改位置初始化、`bit`/`data` 等关键字撞车改名、`fmin`/`round`/`vsnprintf` 等库函数替换、stdbool/stdint shim） | 大（已完成） |
| 菜单数据 | **全部重写**：C99 位置初始化 → C51 位置初始化；演示菜单 → 本项目菜单；GBK 编码 | 中（已完成） |
| 驱动层 | **参照移植（重写中）**：上游 SSD1306（IIC）驱动 + 综合实训实测 C51 软件 I2C 驱动为底子，对齐 framebuffer 接口；diff 引擎保留 | 小~中 |
| 平台层 | **完全重写（已完成）**：STM32 HAL → 16 键 ADC 键盘（官方阈值算法）+ Timer0 20ms | 中 |
| 内存布局 | 字库/图标/字模 → `code` 段（~22KB）；菜单数据/动画状态/显存 → `xdata`；LARGE 内存模型 | 中 |

### 5.2 踩过的坑（已解决）

1. **工程配置**：`MODP2=1`、`RegisterFile=STC8.H` 缺失会导致程序烧录后不运行（实验箱特例）
2. **C51 内存限定符**：`uint8_t xdata` 与 `xdata uint8_t` 的兼容性问题；`extern xdata` 组合必须匹配
3. **`const` 默认进 RAM**：C51 的 `const` 不自动进 code 段，字库必须显式 `code`
4. **内存模型**：Small 模型下 IRAM 256B 是硬约束（函数参数溢出 + 段无法覆盖）；最终 LARGE 模型 + 显式 xdata/code 分工
5. **代码体积**：82KB → 53KB（删 12 个无用图标、8×8/20×20/10×20 字模桩化、15 个未调用函数提取到 oled_extras.c 不链接）
6. **大小写撞名**：`OLED_UI_ShowFPS()` 与 `OLED_UI_ShowFps` 在 C51 大小写不敏感下重复定义 → 变量改名 `OLED_UI_FpsShow`
7. **文件编码**：框架源码 GBK/UTF-8 混合，中文按 GB2312 查字库，菜单数据必须保持 GBK
8. **OLED 初始化参数**（切换屏幕后新坑）：0xA1 段重映射缺失 → 文字左右镜像；0xDA 0x12 COM 配置缺失 → 仅半屏亮；软件 I2C 未优化 → 帧率掉到 ST7920 水平（需位操作内联至 400kHz+）
9. **显示方向**：0xC0/C8 = 上下翻转切换（69 号例程 0xC0 上下颠倒，改 0xC8 正常）；0xA0/A1 = 左右镜像。换屏/换程序时以实际显示为准
10. **实验箱 OLED 接口**：J10 4 针座出厂未焊（只有 J11 7 孔座）；I2C 模式必须改焊 R175/R176（出厂 R173/R174 为 SPI）；插模块前万用表量孔位电压（VCC/GND 反接必烧）

### 5.3 构建状态（v0.2，ST7920 版历史）

```
Program Size: data=15.1  xdata=5229  code=53563
0 Error(s), 8 Warning(s)（L15/L16 多重调用/未调用段警告，可忽略）
输出：Objects\Project.hex（152,847 字节）
```

> OLED 版驱动重写中，构建数据待更新。

---

## 6. 预期项目结构

```
实训项目/
├── Project.uvproj / .uvopt      # Keil C51 工程（MODP2 / STC8.H / LARGE / 3 分组）
├── STARTUP.A51
├── 可行性评估报告.md             # 性能测算与风险评估（OLED 版）
├── 项目介绍.md                   # 本文档
├── docs/agents/                 # Agent 工程配置
├── build/                       # 构建辅助脚本（重建/提取/修复工具）
├── src/
│   ├── inc/
│   │   ├── config.h             # 引脚/按键映射统一配置
│   │   ├── stc8h.h              # STC8H 寄存器定义
│   │   ├── stdint.h / stdbool.h # C51 shim
│   ├── app/
│   │   └── main.c               # 入口：System_Init → OLED_UI_init → OLED_UI_start
│   └── ui/
│       ├── OLED_UI/             # UI 核心（C51 移植）
│       │   ├── OLED_UI.c/.h
│       │   └── OLED_UI_MenuData.c/.h   # 本项目菜单（GBK）
│       ├── OLED_UI_Launcher.c/.h       # 启动封装
│       └── Driver/
│           ├── Hardware_Driver/
│           │   ├── OLED_driver.c/.h    # SSD1306 软件 I2C + diff 引擎（参照移植）
│           │   └── OLED_UI_Driver.c/.h # ADC键盘+Timer0 20ms（已完成）
│           └── Software_Driver/
│               ├── OLED.c/.h           # 图形绘制层（C51 移植）
│               ├── OLED_Fonts.c/.h     # 字模/图标（code 段）
│               └── oled_extras.c       # 未使用函数（不链接，未来启用）
```

---

## 7. 开发里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| v0.1 | ST7920 驱动（8位并口 + 忙检测 + GDRAM 寻址）+ diff 刷新引擎 | ✅（历史，屏幕切换后废弃） |
| v0.2 | OLED_UI 框架 C51 移植（菜单/磁贴/窗口/动画）+ 16 键 ADC 键盘 | ✅（ST7920 版验证框架，HEX 已生成） |
| v0.3 | **屏幕切换 OLED**：SSD1306 软件 I2C 驱动（69 号例程已点亮）+ 虚拟/实体双后端 + 框架复验 | 🔧 驱动已验证，框架复验中 |
| v0.4 | Settings 完整设置流程（亮度真实可调）+ EEPROM 保存 + About 完善 | ⏳ |
| v0.5 | 测温页（复用 NTC 代码） | ⏳ |
| v0.6 | 串口页（行协议文本 + 基础命令） | ⏳ |
| v0.7 | 计算器（16 键键盘映射，键位直读模式） | ⏳ |
| v1.x | 贪吃蛇 → 俄罗斯方块 → 小恐龙（OLED 性能富余，全部可达流畅） | ⏳ |

---

## 8. 上机实测清单（烧录后逐项验证）

1. **烧录**：STC-ISP 打开 `Objects\Project.hex`，**IRC 频率选 24.000MHz**，USB 免驱下载
2. **首屏**：磁贴桌面 6 个图标 + 选择框动画
3. **按键映射确认**（重要）：`src/inc/config.h` 中 `KEY_ADC_UP/DOWN/ENTER/BACK` 当前为占位值 1-4，按实验箱 16 键实际布局调整
4. **OLED 点亮验证**：✅ 已完成（69 号例程，2026-08-14）——0x78 地址；若黑屏先查 ACK（驱动含探测），再查 0xA1 镜像/0xDA 半屏/电荷泵三项初始化；**上下颠倒改 0xC0↔0xC8**
5. **虚拟 OLED 调试**（无实体屏路径）：驱动 `VIRTUAL_OLED=1` 编译 → AiCube-ISP `工具→调试仿真接口→OLED-128*64` 打开虚拟屏，调菜单/动画/字模
6. **性能实测**：Settings 开"显示帧率"，全屏动画/菜单移动帧率应达 40fps+（**须实体屏验证**，虚拟屏 FPS 失真）；软件 I2C 速率影响帧率，必要时内联优化
7. **亮度调节实测**：设置页调亮度，0x81 对比度应实时变化
8. **深浅色主题**：切换后屏幕反色（0xA7/A6）

---

## 9. 关键技术点总结（学习价值）

1. **OLED 驱动**：软件 I2C 时序、SSD1306 页寻址、初始化序列（0xA1 段重映射 / 0xDA COM 配置 / 0x8D 电荷泵）
2. **framebuffer + diff 局部刷新**：受限总线上的 UI 刷新策略设计（ST7920 时代为必需，OLED 上为保底加速）
3. **嵌入式 UI 框架移植**：C99 → C89 子集、内存模型选择、代码体积裁剪
4. **C51 特性与坑**：内存限定符、const 语义、关键字撞名、大小写规则
5. **模拟量输入**：ADC 键盘（分压识别）、NTC 查表+线性插值测温
6. **串口通信协议**：行协议解析、命令路由、环形缓冲

---

## 10. 相关文档

| 文档 | 内容 |
|---|---|
| [可行性评估报告.md](可行性评估报告.md) | 性能测算（OLED 版帧率/耗时/内存）、逐功能评估、问题清单 |
| [docs/agents/STC8H8K64U实验箱硬件设备.md](docs/agents/STC8H8K64U实验箱硬件设备.md) | 实验箱 9.62 硬件资源总表 |
| 上游 OLED_UI（Apache 2.0） | [github.com/bdth-7777777/OLED_UI](https://github.com/bdth-7777777/OLED_UI)，本地镜像 `../OLED_UI_upstream/` |
