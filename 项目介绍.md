# 电子手表风格 UI 菜单系统 —— 项目介绍

> 基于 STC8H8K64U 实验箱 9.62 与 0.96" SSD1315 OLED（128×64）液晶屏的综合实训项目
> 文档版本：v0.5（2026-08-18：SPI 显示 + 红外遥控完成，随机状态根治，虚拟屏修复，实测数据已同步更新）

---

## 1. 项目概述

本项目以 STC8H8K64U 单片机实验箱为平台，利用 **0.96 寸 SSD1315 OLED 屏（128×64，硬件 SPI @6MHz）**，构建一套**电子手表风格**的流畅 UI 菜单系统。系统通过实验箱的 **16 键 ADC 键盘 + 红外遥控**（双输入，键盘兜底）操作（上/下/确认/返回），以"磁贴桌面 + 多级列表菜单"的形式整合多项实用功能。

### 项目亮点

- **流畅的 UI 交互**：framebuffer + **页级 diff 刷新引擎**——SSD1315 硬件 SPI @6MHz（官方 69 号例程 SPI 版移植），全屏刷新 <1ms，diff 引擎只发变化页，**静态 ~90fps、动画 ~70fps**（实测，无 I2C 总线锁死故障模式）
- **SPI 显示后端**：实验箱出厂 R173/R174 焊好，SSD1315 7 针模块直插 J11 座（零焊接）；SPI 全屏刷新 <1ms 且无 I2C 偶发失败问题（历史 I2C 400kHz 卡死根因）
- **显示容错**：SPI 发送超时保护（~1ms，防硬件异常无限等待）+ 周期全刷自愈；驱动层保留 I2C 后端可切换（`OLED_IF_SPI` 宏）
- **完整的多组件 UI**：磁贴桌面（首屏）、列表菜单、滑动/渐隐动画、滚动条、光标高亮
- **深度定制的中文界面**：GB2312 12×12 / 16×16 双档中文字模（补 29 字），菜单全中文
- **功能代表图标**：6 个磁贴图标全部手绘定制（齿轮/温度计/DB9 串口/计算器/游戏手柄/关于），一眼可辨功能
- **模块化分层架构**：驱动层 / UI 框架层 / 应用页三层解耦，新增功能 = 新增一个菜单页
- **性能仪表盘**：帧率显示默认开启，实时展示系统刷新率（可关）
- **双后端调试**：实体 OLED（SPI）/ 虚拟 OLED（USB-CDC → STC-ISP 调试仿真接口）`VIRTUAL_OLED` 编译开关切换——同学无 OLED 模块也能开发

---

## 2. 硬件平台

### 2.1 主控芯片：STC8H8K64U

- 8051 内核（1T 增强型），主频 24MHz
- 64KB Flash 程序存储，8KB SRAM（xdata），256B IRAM
- 内置 EEPROM（IAP 方式）、12 位 ADC（15 通道）、USB 2.0-FS
- 3 个定时器、UART×4、**硬件 I2C/SPI** 等丰富外设
- **MDU16 硬件数学单元**：乘除/开方硬件加速（计算器可选使用）

### 2.2 使用的实验箱外设

| 外设 | 接线/通道 | 用途 |
|---|---|---|
| 0.96" SSD1315 OLED（128×64） | **硬件 SPI @6MHz**：SCLK=P2.5、MOSI=P2.3、RES=P2.4、DC=P3.4、CS=P1.1（J11 7 孔座直插） | 主显示 |
| 16 键 ADC 键盘 | P1.0 / ADC 通道 0（电阻分压式，256 分档） | 输入设备（兜底） |
| NTC 热敏电阻 | P1.3 / ADC 通道 3（MF52 10K@25°C，B=3950） | 测温功能（v1 规划） |
| 蜂鸣器（有源） | P5.4（=复位脚，需 ISP 勾"复位脚用作 I/O 口"） | 按键提示音（已实现，设置页可关） |
| 红外接收头 U7 | P3.5（T1 100µs 采样，NEC 协议） | 红外遥控（已实现，遥控为主） |
| USB | CDC 免驱 | 程序下载 + 虚拟 OLED 调试 + 串口通信（v1 规划，共用 CDC 通道） |

**实体屏接线（2026-08-17 实测，SPI 模式）**：
- SSD1315 7 针模块直插 J11 7 孔座 SPI 位（1=GND 2=VCC 3=SCLK 4=D1(MOSI) 5=RES 6=DC 7=CS），实验箱出厂 R173/R174（SPI 档）已焊，**零焊接**
- 7 针模块在实验箱上只能反插 → 驱动已配 **180° 旋转**（0xA0 段重映射关 + 0xC0 COM 正向）；若换模块方向不对：左右镜像改回 0xA1、上下颠倒改回 0xC8
- I2C 后端保留（`OLED_IF_SPI=0` + 改焊 R175/R176 或飞线），仅作备用

---

## 3. 功能详述

### 3.1 交互设计通则（全局操作规范）

所有界面遵循同一套按键逻辑（**ADC 键盘 + 红外遥控双输入**，`Key_Get*Status = 键盘 || 遥控`，遥控为主/键盘兜底）：

| 操作 | 键 | 行为 |
|---|---|---|
| 上 / 下 | 键 1 / 2 或遥控 ▲▼ | 菜单项/磁贴移动；**长按 0.8s 后连发**（10 键/秒），支持快速滚动 |
| 确认（进入） | 键 3 或遥控 OK/▶ | 进入子菜单 / 触发菜单项回调 / 切换开关项 |
| 返回 | 键 4 或遥控 ◀ | 返回上级菜单 |

> 键盘映射已实测确认（config.h 的 KEY_ADC_* 可调）；遥控键码硬编码（IR 映射表，换遥控器需改码重烧）。

**按键处理链路**（20ms 节拍驱动）：
```
Timer0 20ms 中断
 ├─ Driver_KeyScan()：ADC 采样 → 256 分档 → 三态滤波（连续 3 次相同才确认，60ms 去抖）
 ├─ Driver_IRScan()：红外轮询（T1 100µs 采样中断解 NEC 帧 → 逻辑键）
 ├─ Buzzer_Tick()：按键音节拍（60ms 自动关，SoundEnable 开关）
 └─ OLED_UI_InterruptHandler()：按键状态记录 → 抬起沿检测 → 长按连发 → 窗口/菜单逻辑
主循环：MoveMenuElements（动画插值 + 渲染）→ OLED_Update（diff 刷新）
```

**全局交互特性**：
- 页面切换带**渐隐动画**（5 级棋盘格遮罩，40ms/级）+ **滑入动画**（页面从屏幕侧移入，UNLINEAR 指数逼近，SPEED=8 下 ~1.5s 完成）
- 深浅色主题全局生效（SSD1306 0xA7 反色指令），不改变布局
- 按键提示音：已实现（P5.4 蜂鸣器，键盘+遥控按下统一短鸣；设置页"提示音"开关，默认开）

### 3.2 主界面（磁贴桌面，开机首屏）✅

```
┌────────────────────────────────┐
│  ⚙设置   ℹ关于   🌡测温       │
│  ⏚串口   ┃计算器  🎮游戏      │
└────────────────────────────────┘
    6 磁贴横向滑动宫格 + 底部文字 + 滚动条 + 指示箭头
```

- **6 个功能磁贴**（32×32 手绘图标，`code` 段）：

| 磁贴 | 图标 | 说明 |
|---|---|---|
| 设置 | ⚙ 齿轮（Image_gear） | 8 径向梯形齿 + 细环壁 + 中心孔（emoji 风格） |
| 关于 | ℹ 信息（Image_more） | 三点省略号 |
| 测温 | 🌡 温度计（Image_thermo） | 管身 + 球泡 + 液柱 |
| 串口 | ⏚ DB9 连接器（Image_serial） | 梯形外壳 + 引脚点 |
| 计算器 | 计算器（Image_calc2） | 外框 + 屏幕 + 6 键 |
| 游戏 | 🎮 手柄（Image_gamepad） | 主体 + 十字键 + 双按钮 |

- **磁贴滑动动画**：UNLINEAR 指数逼近（无过冲——曾用 PID 曲线出现过冲回摆，已改），磁贴从屏幕外滑入、切换时整排平移
- **指示箭头**：屏幕中央顶部固定位置（Arrow1 36×36），随磁贴移动指示当前选中项
- **底部滚动条**：指示当前选中位置（宽度随页数比例）
- 磁贴图标缺失时自动显示 UnKnown 占位图标（框架内置容错）
- 底部当前磁贴名称 + 选中框（NOT_SHOW 风格，磁贴本身即高亮）

### 3.3 设置页 Settings ✅

纵向列表菜单，5 项，12×12 中文字体，行前缀指示（`*`=开 / `-`=关 / `>`=子菜单 / `~`=回调）：

| 设置项 | 行为 | 状态 |
|---|---|---|
| 深浅色模式 | 切换 `ColorMode` → SSD1306 **0xA7（反色）↔ 0xA6（正常）**，整屏即时反色 | ✅ |
| 显示帧率 | 切换 `OLED_UI_FpsShow` → 屏幕右上角 6×8 小字实时显示刷新率（**默认开启**） | ✅ |
| 语言（占位） | 切换中/英文（框架具备双语字模，逻辑待接） | ⏳ 占位 |
| 提示音 | 蜂鸣器开/关（P5.4 驱动已接，按键统一短鸣） | ✅ |
| [返回] | 返回主界面（渐隐 + 滑出动画） | ✅ |

### 3.4 关于页 About ✅

静态信息列表（只读）：项目名称（电子手表式 UI 菜单）、主控 MCU（STC8H8K64U @24MHz）、屏幕（0.96" SSD1306 OLED）、框架（OLED_UI 0.2.1）、开源协议（Apache 2.0）、[返回]。

### 3.5 NTC 测温 ⏳ v1（已定案）

| 项目 | 方案 |
|---|---|
| 数据来源 | P1.3 / ADC 通道 3（MF52 10K@25°C，B=3950），12 位 ADC |
| 温度换算 | 161 点温度-ADC 查表 + 二分查找 + 线性插值（复用实验十四 `ntc.c`，整数×10 定点） |
| 界面布局 | 上半屏大字号温度值（16×16"温度" + 8×16 数字），下半屏状态栏（简洁版：不记最大/最小，用户定案） |
| 刷新策略 | 1s 采样一次，只刷数字区域（页级 diff，<0.5ms） |
| 量程 | -40 ~ +120°C |

### 3.6 串口通信 ⏳ v1（已定案：USB-CDC）

- **物理层**：**USB-CDC 虚拟串口**（实验箱 USB 座 P3.0/P3.1 直连 PC，串口助手显示为 COM 口）——不用 UART2/DB9（DB9 需转接线且 UART2 无现成排针）
- **功能**：PC 发送 → OLED 终端式滚动显示（**不保存、不写 EEPROM**）；`\n` 分行 + 超宽**自动换行**（用户定案）；底部状态行（连接状态/接收计数）
- **可选双向**：MCU→PC 回显（USB 天然双向）
- **移植参考**：官方 61 号（CDC 协议）+ 70.2 号（串口绘图 USB-CDC）
- **注意**：VIRTUAL_OLED=1 开发期与虚拟 OLED 可能争用 USB（按实体屏模式设计）

### 3.7 数字计算器 ⏳ v1（已定案）

- **键盘映射**（16 键完整覆盖——键盘或遥控双输入均可）：

| 1 | 2 | 3 | + |
|---|---|---|---|
| 4 | 5 | 6 | − |
| 7 | 8 | 9 | × |
| 0 | C | = | ÷ |

- **运算精度**（用户定案）：**定点 2 位小数**（结果×100 整数运算，显示 3.50；7÷3=2.33），除零/溢出显示 Error
- **界面**：上方表达式行，下方结果行；连续运算、清屏
- 可选用 MDU16 硬件乘除加速

### 3.8 倒计时闹钟 ⏳ v1（已定案）

- **计时**（用户定案）：Timer 秒计数自算（每秒减 1，可暂停/重置，归零蜂鸣 P5.4）——不用 RTC 闹钟（换算/暂停逻辑复杂）
- **界面**：8×16 大数字倒计时显示 + 设定（分/秒步进）+ 运行/暂停/重置状态
- **按键音联动**：所有按键（键盘+遥控）按下统一短鸣（设置页"提示音"开关，默认开）

### 3.9 时钟/万年历 ⏳ v1（已定案：硬件 RTC）

- **时间源**（用户定案）：**STC8H 内部 RTC + 实验箱 32.768K 晶振**（官方 50 号例程移植）——比软件计时准，答辩硬核知识点
- **显示**：时:分:秒 大数字 + 年/月/日/星期（蔡勒公式或查表）；主界面磁贴"时钟"进入
- **断电**：EEPROM 存时间戳，上电恢复
- **联动**：与闹钟/设置共用 IAP 驱动

### 3.10 红外遥控 ✅（已完成：硬编码键码 + 键盘兜底）

- **硬件**：接收头 U7 → P3.5；**T1** 做 100µs 采样中断跑 NEC 解码（官方 29 号例程 Coody 算法移植，Timer0 已被 UI 占用）
- **键码**（用户定案）：**硬编码**——开发期读码页读一次遥控器实际键码 → 写入映射表（省对码 UI/EEPROM 存码 ~0.8KB；换遥控器需改码重烧）✅ 已实现
- **键位映射**（用户遥控器：数字 0-9 + `*` `#` + ▲▼◀▶ + OK）：
  - 菜单：▲▼=上下、OK/▶=确认、◀=返回
  - 计算器：0-9=数字、`*`=×、`#`=÷、▲=+、▼=−、◀=C、OK==
  - 游戏：▲▼◀▶=方向、OK=暂停
- **输入架构**（用户定案，方案 A）：**遥控为主 + ADC 键盘保留兜底**，`Key_Get*Status = 键盘 || 遥控`——答辩遥控失灵有救生圈，开发期无遥控器也能调试 ✅ 已实现
- **按键音联动**：遥控按键按下统一蜂鸣（红外无物理反馈，按键音是必要反馈）✅ 已实现（长按连发节流 300ms）
- **T1 使用**：100µs 采样中断（1T 模式，16 位重载）；UART2（T2 定时）与红外不冲突

### 3.11 游戏 ⏳ v2（已定案：贪吃蛇 + 2048）

| 游戏 | 玩法 | 界面方案 | 性能方案 |
|---|---|---|---|
| 贪吃蛇 | 方向键控制，吃豆增长，撞墙/咬尾结束 | 全屏 16×8 网格（8px 格） | 每帧只刷蛇头/蛇尾 2 格（<0.5ms） |
| 2048 | 4×4 数字合并（2/4 随机生成，Timer 扰动伪随机），方向键滑动 | 4×4 棋盘（32px 格） | 每步只刷变化格子 |

- **最高分**（用户定案）：**不存 EEPROM**（运行期记录，断电清零）
- 游戏入口：磁贴"游戏"→ 游戏列表子菜单（贪吃蛇/2048）；`[返回]` 退出；暂停功能（OK 键）

### 3.12 功能状态总览

| 功能 | 状态 | 说明 |
|---|---|---|
| 磁贴主界面 | ✅ | 6 磁贴 + 手绘图标 + 滑动动画 |
| 多级列表菜单 | ✅ | 上下/确认/返回 + 长按连发 |
| 设置（深浅色/帧率） | ✅ | 深浅色 0xA7/A6 反色切换；帧率默认开 |
| 关于 | ✅ | 信息展示 |
| 页面切换动画 | ✅ | 渐隐 + 滑入（SPEED 8，~1.5s） |
| 帧率优化 | ✅ | SPI 6MHz + 页级 diff + 静态跳过重绘（静态 ~90fps） |
| 显示容错 | ✅ | SPI 发送超时保护 + 周期全刷自愈 |
| 虚拟 OLED 调试 | ✅ | USB-CDC → STC-ISP 虚拟屏（实测正常） |
| NTC 测温 | ⏳ v1 | 定案：P1.3/ADC3 查表，简洁状态栏（无最大/最小） |
| 串口通信 | ⏳ v1 | 定案：USB-CDC 虚拟串口，自动换行，不保存 |
| 倒计时闹钟 | ⏳ v1 | 定案：Timer 秒计数自算 + 蜂鸣器 + 按键音联动 |
| 计算器 | ⏳ v1 | 定案：定点 2 位小数 + 除零/溢出 Error |
| 时钟/万年历 | ⏳ v1 | 定案：硬件 RTC（32K 晶振）+ EEPROM 时间戳恢复 |
| 红外遥控 | ✅ | 完成：T1 NEC 解码 + 遥控为主/键盘兜底 + 按键音 + 长按节流 |
| 游戏（贪吃蛇 + 2048） | ⏳ v2 | 定案：方向键/遥控操作，最高分运行期记录不存 |

> ✅ 已完成　⏳ 已规划　💤 预留

---

## 4. 系统架构

### 4.1 分层结构（OLED_UI C51 移植版）

```
┌─────────────────────────────────────────────────┐
│ 应用页层（菜单数据 + 回调）                        │
│  OLED_UI_MenuData.c —— 磁贴主屏 + 设置/关于/占位页  │
├─────────────────────────────────────────────────┤
│ UI 框架层（上游 OLED_UI 核心，C51 移植）           │
│  OLED_UI.c —— 菜单树/动画/窗口/按键状态机          │
│  OLED_UI_Launcher.c —— 启动封装 + Timer0 中断     │
│  OLED.c —— 图形绘制层（framebuffer 操作）          │
│  OLED_Fonts.c —— 中英文字模 + 图标（code 段）      │
├─────────────────────────────────────────────────┤
│ 平台适配层（STC8H8K64U 版）                       │
│  OLED_driver.c —— SSD1315 硬件 SPI/I2C 双后端     │
│               + 页级 diff + 超时保护 + 虚拟后端    │
│  OLED_UI_Driver.c —— 16键ADC键盘 + 红外遥控(T1)   │
├─────────────────────────────────────────────────┤
│ 底层：STC8H 头文件 + stdint/stdbool/c51lib shim   │
└─────────────────────────────────────────────────┘
```

### 4.2 运行模型（事件驱动 + 定时节拍）

```
Timer0 20ms 中断 ──► Driver_KeyScan（ADC 键盘三态滤波）+ Driver_IRScan（红外）
                  └─► OLED_UI_InterruptHandler（按键记录/长按连发/帧率计数）
T1 中断（100µs）───► NEC 帧采样解码（红外）
主循环 OLED_UI_MainLoop：
  按键状态机（上/下移动、确认/返回边缘检测）
      │
  动画插值（UNLINEAR 指数逼近 / PID 曲线）
      │
  渲染到 framebuffer（OLED_DisplayBuf，1KB xdata）
      │
  OLED_Update() —— 页级 diff 对比影子缓冲，只发变化页 → SSD1315
```

- **不在中断里刷屏**：SPI/I2C 传输在中断外执行，主循环独占显示总线
- 上下键长按连发由框架内置（PRESS_TIME 0.8s → 连发）

### 4.3 SSD1306 驱动与 diff 刷新引擎

- **硬件 SPI @6MHz**：STC8H SPI1（引脚组 1，4T=6MHz，模式 0）——全屏 1024 字节 <1ms（I2C 400kHz 时代 ~23ms，快 20 倍以上）；RES(P2.4)/DC(P3.4)/CS(P1.1) GPIO 控制
- **页级 diff**：驱动维护 1KB 影子缓冲，逐页（8 行×128 列）memcmp 比较，只发变化页（SPI 下整页发送事务少，无 I2C 失败率问题）
- **周期全刷自愈**：每 50 帧强制全刷一次，防偶发丢字节残留（视觉不可见）
- **SPI 超时保护**：单字节发送 ~1ms 超时，SPI 硬件异常时避免无限等待卡死
- **双后端驱动**：
  - 接口选择：`OLED_IF_SPI` 宏（1=SPI 当前 / 0=I2C 备用）
  - 显示选择：`VIRTUAL_OLED` 宏（1=虚拟 OLED，经 USB-CDC 整屏发到 STC-ISP 调试仿真接口；0=实体屏）
- **深浅色主题**：0xA7（反色）↔ 0xA6（正常）

### 4.4 按键驱动（键盘 + 红外双输入）

- **ADC 键盘**：P1.0 / ADC 通道 0，电阻分压 16 档（每档 256 ADC 值，±64 容差）；**三态滤波**（连续 3 次采样同一键才确认，60ms 去抖，官方 Coody 算法）；查询式转换带超时保护（0.5ms，防中断死等）
- **红外遥控**：T1 100µs 采样中断解 NEC 帧（SYNC+32 位：用户码+数据码+反码校验）；`IR_KeyToLogical` 映射到 KEY_ADC_* 逻辑键；长按连发节流 300ms；`Key_Get*Status = 键盘 || 遥控`

---

## 5. 移植记录（上游 OLED_UI → C51/STC8H）

### 5.1 移植范围

| 层 | 处理 | 工作量 |
|---|---|---|
| UI 核心 OLED_UI.c/.h | **C51 兼容化 39 处**：块内声明提升（含 for 初始化区）、C248 结构体初始化分离、fmin/vsnprintf 替换、patterns 提升 static code、HAL 删除、`OLED_UI_ShowFps` 撞名改名、include 路径 | 大（✅） |
| 菜单数据 | **全部重写**：C99 位置初始化 → C51 位置初始化；本项目菜单；GBK 编码；List_MenuArea 修复 | 中（✅） |
| 图形层 OLED.c | C51 兼容 + **ShowImageArea 整列快路径 + ClearArea 页级掩码**（8 倍加速） | 中（✅） |
| 字库 OLED_Fonts.c | const→code；**裁剪**（Gif/LOGO/QR/32 版图片/20 号字体，70KB→56KB）；**补 29 字**（SimSun 点阵渲染）+ **5 个手绘图标** | 中（✅） |
| 驱动层 OLED_driver.c | **重写**：硬件 SPI（6MHz）+ I2C 双后端 + 页级 diff + 周期全刷 + 超时保护 + 虚拟 OLED | 大（✅） |
| 平台层 OLED_UI_Driver.c | **完全重写**：16 键 ADC 键盘（三态滤波）+ 红外遥控映射 + Timer0 20ms + GetTick + 按键音 | 中（✅） |
| 工程配置 | MODP2=1、RegisterFile=STC8.H、无 STARTUP.A51、LARGE、PRINTF_LARGE、3 分组 | ✅ |

### 5.2 构建状态（当前，2026-08-18）

```
Program Size: data=15.5  xdata=3808  const=6528  code=42760（Flash 共 48.1KB / 64KB，剩 15.9KB）
0 Error(s), 2 Warning(s)
  - L15 MULTIPLE CALL ×2：中断与主循环共享函数（框架架构使然，已知可接受）
输出：Objects\Project.hex
```

### 5.3 踩过的坑（已解决，新会话必读）

1. **工程配置**：`MODP2=1`、`RegisterFile=STC8.H` 缺失 → 程序烧录后不运行（实验箱特例）
2. **I2CCFG 的 MSSPEED 是 bit5-0 等待时钟数**：速度 = `FOSC/2/(MSSPEED×2+4)`——0xe0 在 24MHz 只有 176kHz（官方 0xe0 是 11.0592MHz 的 100kHz 配置）！400kHz 需 MSSPEED=13 → **0xCD**
3. **I2C 总线偶发失败会"冻结"**：每事务 13ms 超时累积 → 画面冻结——修复：超时缩短 + GPIO 9 脉冲总线恢复 + SSD1306 重初始化
4. **列级 diff 的丢字节残留**：偶发丢字节后缓冲已同步、diff 不重刷 → 噪点残留——修复：周期全刷自愈
5. **C51 声明位置**：块内任意位置声明报错；连"赋值语句后声明"都报 C141；for 初始化区声明不支持；结构体不能声明时用对象/函数返回值初始化（C248）
6. **未引用 const 数组不会被 REMOVEUNUSED 移除**——必须手动删（字库裁剪是装进 64KB 的关键）
7. **补字模**：SimSun 16px/12px 有内嵌点阵——大画布渲染 + bbox 居中 + **1:1 不缩放**（缩放有灰阶 → 断笔）；格式为**列式**（每列垂直 8bit，先上半页再下半页）
8. **ClearArea 页级掩码**：rows 必须限制在本页剩余行（曾跨页清错位 → 界面残留，误判为按键失效）
9. **SP 不要手动设**：C51 运行库已设 SP=0x22（栈 221B），手动 0x80 砍栈到 128B 易溢出
10. **文件编码**：框架源码 GBK/UTF-8 混合；**不要用 Edit 工具改 GBK 文件**（会把中文注释写坏）——用 python rb/wb 同编码处理
11. **大小写撞名**：`OLED_UI_ShowFPS()` 与 `OLED_UI_ShowFps` 变量在 C51 大小写不敏感下冲突 → 改名 `OLED_UI_FpsShow`
12. **OLED 方向**：上下 0xC0↔0xC8、左右 0xA0↔0xA1（SPI 版 7 针模块反插 → 180° 旋转 0xA0+0xC0）
13. **无 STARTUP.A51 参与编译**：库启动只拷贝初始化段、**不清无初值变量** → 所有全局/static 必须显式初始化（曾致进页卡死/行为时好时坏，已根治）
14. **grep 中文字符 `\b` 无效**（GBK 无单词边界）——检查字库引用必须单字全量比对（曾误删"屏"字模）
15. **虚拟屏发送不要查 `bUsbInBusy`**：新版 STC-ISP 下忙标志恒 true → 永不发送（已改为无条件发送；开发时调试仿真接口窗口必须打开）

---

## 6. 项目结构

```
实训项目/
├── Project.uvproj                 # Keil C51 工程（MODP2 / STC8.H / LARGE / 3 分组）
├── 可行性评估报告.md               # 性能测算与风险评估
├── 项目介绍.md                     # 本文档
├── HANDOFF.md                     # 会话交接文档（踩坑清单）
├── src/
│   ├── inc/
│   │   ├── config.h               # 引脚/按键/显示后端统一配置
│   │   ├── c51lib.h               # C51 库函数原型 shim（防 STC 增强头污染）
│   │   ├── stdint.h / stdbool.h   # C51 shim
│   ├── app/
│   │   └── main.c                 # 入口：System_Init → OLED_UI_init → OLED_UI_start
│   └── ui/
│       ├── OLED_UI/               # UI 核心（C51 移植）
│       │   ├── OLED_UI.c/.h
│       │   └── OLED_UI_MenuData.c/.h   # 本项目菜单（GBK）
│       ├── OLED_UI_Launcher.c/.h       # 启动封装 + Timer0 中断
│       └── Driver/
│           ├── Hardware_Driver/
│           │   ├── OLED_driver.c/.h    # SSD1315 SPI/I2C 双后端 + 页级 diff + 虚拟OLED
│           │   ├── OLED_UI_Driver.c/.h # ADC键盘 + 红外映射 + Timer0 20ms + 按键音
│           │   └── IR_Remote.c/.h      # 红外 NEC 解码（T1 100µs 采样）
│           └── Software_Driver/
│               ├── OLED.c/.h           # 图形绘制层
│               └── OLED_Fonts.c/.h     # 字模/图标（code 段）
```

---

## 7. 开发里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| v0.1 | ST7920 驱动 + diff 刷新引擎 | ✅（历史，屏幕切换后废弃） |
| v0.2 | OLED_UI 框架 C51 移植（菜单/磁贴/窗口/动画）+ 16 键 ADC 键盘 | ✅（ST7920 版验证框架） |
| v0.3 | **屏幕切换 OLED + 框架完整移植**：硬件 I2C 400kHz、列级 diff、总线容错、实体屏实测静态 80fps/动画 35fps、按键全正常 | ✅ |
| v0.4 | Settings 完整设置流程（深浅色/帧率）+ About + 页面动画优化 | ✅ |
| v0.5 | **红外遥控**（T1 采样解码 + 遥控控菜单 + 键盘兜底 + 按键音 + 长按节流） | ✅ |
| v0.6 | NTC 测温 + 计算器（定点 2 位小数） | ⏳ |
| v0.7 | 倒计时闹钟（蜂鸣器+按键音）+ 时钟/万年历（硬件 RTC） | ⏳ |
| v0.8 | 贪吃蛇 + 2048 | ⏳ |
| v0.9 | 串口（USB-CDC，自动换行） | ⏳ |
| v1.0 | 收尾：EEPROM 设置保存 + 字库补字 + 上板全流程验证 | ⏳ |

---

## 8. 上机实测记录（2026-08-17/18，SPI 版）

| 项目 | 结果 |
|---|---|
| 实体屏点亮 + 显示方向 | ✅ SSD1315 J11 直插，180° 旋转（0xA0+0xC0）正常 |
| 主屏磁贴 + 图标 | ✅ 6 磁贴 + 手绘图标正常 |
| 按键导航（键盘上/下/确认/返回） | ✅ 全正常（键值 1/2/3/4） |
| 红外遥控 | ✅ T1 NEC 解码 + 遥控控菜单 + 长按节流 300ms |
| 页面切换（设置/关于） | ✅ 渐隐 + 滑入动画，~1.5s |
| 深浅色切换 | ✅ 0xA7/A6 即时反色（翻转立即生效） |
| 帧率显示 | ✅ 默认开启，右上角显示 |
| **静态帧率** | ✅ **~90fps** |
| **动画帧率** | ✅ **~70fps** |
| 进页卡死/行为不稳定 | ✅ 已根治（无初值变量显式初始化，0bd0d48） |
| 开关项立即生效 | ✅ 深浅色/帧率/提示音按确认即变（3fafbda） |
| 虚拟 OLED 调试 | ✅ USB-CDC → STC-ISP 虚拟屏（无条件发送修复后正常） |

---

## 9. 关键技术点总结（学习价值）

1. **硬件 SPI 显示**：SPI1 4T=6MHz 全屏 <1ms（对比 I2C 400kHz ~23ms）；RES/DC/CS GPIO 控制；模式 0 MSB 先
2. **framebuffer + 页级 diff**：受限总线上的 UI 刷新策略（页级 → 周期全刷自愈 → 静态跳过重绘）
3. **显示容错**：SPI 发送超时保护、周期全刷自愈——嵌入式可靠性设计（历史 I2C 总线恢复方案保留为备用后端）
4. **嵌入式 UI 框架移植**：C99 → C89 子集、内存模型、代码体积裁剪（70KB→56KB）
5. **C51 特性与坑**：内存限定符、const 语义、声明位置规则、关键字撞名、大小写
6. **点阵字模生成**：SimSun 内嵌点阵 1:1 提取（无灰阶）、OLED 列式存储格式
7. **模拟量输入**：ADC 键盘（分压识别 + 三态滤波）、NTC 查表测温（规划）
8. **红外 NEC 解码**：T1 100µs 采样中断、SYNC+32 位数据+反码校验、逻辑键映射——遥控/键盘双输入架构
9. **C51 变量初始化铁律**：无 STARTUP.A51 时无初值全局=随机——所有全局/static 显式初始化（随机状态根治）

---

## 10. 相关文档

| 文档 | 内容 |
|---|---|
| [可行性评估报告.md](可行性评估报告.md) | 性能测算、逐功能评估、问题清单 |
| [HANDOFF.md](HANDOFF.md) | 会话交接 + 完整踩坑清单 |
| [docs/agents/STC8H8K64U实验箱硬件设备.md](docs/agents/STC8H8K64U实验箱硬件设备.md) | 实验箱 9.62 硬件资源总表 |
| 上游 OLED_UI（Apache 2.0） | [github.com/bdth-7777777/OLED_UI](https://github.com/bdth-7777777/OLED_UI)，本地镜像 `../OLED_UI_upstream/` |
