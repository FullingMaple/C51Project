# 计算器功能开发指南（AI 协作版）

> 任务：在现有项目中实现**计算器页面**。
> **你不写代码**——AI 写，你负责：给 AI 派活 → 监督改动 → 虚拟屏验证 → 提交。
> 预计 2-3 天（比 NTC 复杂：多 2 个文件改动 + 运算逻辑）。
> 有任何不懂的术语，直接问 AI（把本指南发给它）。

---

## 开工指引（第一次拿到项目，照做）

1. 用VsCode打开项目文件夹
2. **切到自己的分支**（你是计算器负责人）：
   ```bash
   git checkout feature/calc
   ```
3. **读本指南**：§0（工作方式 + 分支铁律 + Skill 用法）→ §2（环境速通）
4. **开工**：跑 `/c51-intellisense` + `/setup-matt-pocock-skills`（Skill 见 §0）→ 按 §2 切虚拟屏 → 给 AI 任务书（§4）
5. **完成**：验证清单全过 → 推分支 → 通知主开发合入：（交给AI）
   ```bash
   git push origin feature/calc
   ```

---

## 0. 工作方式（先读这个）

```
循环：给 AI 任务书（§4） → AI 改代码 → 你检查（§5）→ 构建 → 虚拟屏验证（§6）
      → 有问题把现象回喂 AI → 循环，直到验证清单全过 → 提交（§9）
```

- AI 是 VSCode 里的 Claude 插件（DeepSeek 模型），能直接改文件、跑命令
- **每次 AI 改完代码后，你都要：① 看它改了哪些文件（§5 监督清单）② 构建 ③ 虚拟屏看效果**
- 不信任 AI 的"我觉得没问题"——一切以**构建通过 + 虚拟屏实测**为准

### ⚠️ 分支铁律（开工第一步，最重要！）

**你必须在自己的分支 `feature/calc` 上开发**——你和另一位同学同时开发（他做 NTC 测温），各改各的分支，最后主开发统一合入：

```bash
# 开工第一步（只做一次）：创建并切换到你的分支
git checkout -b feature/calc

# 每次提交前确认你在正确的分支上（应显示 * feature/calc）
git branch

# 如果发现自己跑到了 dev 分支上——立刻切回来
git checkout feature/calc
```

- **绝不直接提交到 dev / main 分支！**（两位同学 + 主开发共用一个仓库，混在一起会互相踩）
- 看到 `git branch` 输出 `* dev` 时**不要提交**——先 `git checkout feature/calc`
- AI 如果擅自 `git commit` 到别的分支——让它先切回 `feature/calc`

### 使用这些 Skill（重要，主开发已配好）

在 VSCode 对话里输入 `/skill名` 即可调用（你的 Claude 插件里已有）：

**① 项目开始前（第一次打开工程，只做一次）**：

| Skill | 用途 | 为什么要用 |
|---|---|---|
| `/c51-intellisense` | 配置 VSCode 代码智能提示（悬停看函数说明、点击跳转定义） | C51 的 `sfr/sbit` 等关键字默认引擎解析不了，不配的话 AI 和你看代码都没提示，容易瞎改 |
| `/setup-matt-pocock-skills` | 搭建工程协作配置（问题追踪/文档规范约定） | 团队协作的"规矩"文件，主开发已配好，你调用一次确认配置就位 |

**② 项目开发中（随时可调用）**：

| Skill | 用途 | 什么时候用 |
|---|---|---|
| `/grill-me` | 让 AI 对你的想法/需求"拷问"（逐轮提问把设计想清楚） | **让 AI 动手前**——把"我想做 XX"丢给它，它会追问细节（界面/边界/异常），想清楚了再写代码，少返工 |
| `/tdd` | 测试驱动开发（先写测试、再实现） | 纯逻辑功能（计算器运算/优先级/溢出检查）特别适合——让 AI 先写测试验证逻辑正确，再写实现 |
| `/handoff` | 生成/读取会话交接文档（HANDOFF.md） | **每次开发结束**——让 AI 把进度/踩坑写进 HANDOFF；下次开会话先让它读 HANDOFF 继续，不丢上下文 |

**用法示例**（对 AI 说）：
- 开工前：`/grill-me`，然后说"我要做计算器页，这是我的任务书"
- 让 AI 写运算逻辑时：`/tdd`，然后说"给计算器表达式解析写测试（12+34×5=182 等用例）"
- 收工前：`/handoff`，然后说"总结今天进度写入 HANDOFF"

---

## 1. 任务总览

| 项 | 内容 |
|---|---|
| 功能 | 主界面"计算器"磁贴 → 进入计算器页：按键输入表达式 → 显示结果 |
| 输入 | 实验箱 16 键键盘（键码→数字/运算符）+ 红外遥控（0-9/*/#/▲▼◀▶/OK） |
| 显示 | 虚拟 OLED 屏（VIRTUAL_OLED=1，画面在 STC-ISP 窗口里） |
| 验收 | 见 §6 验证清单（10 项全过） |

**你只允许改动 4 个文件**（其余文件一律不许动！必要的测试脚本除外）：

| 文件 | 改动 | 编码 |
|---|---|---|
| `src/ui/OLED_UI/OLED_UI_MenuData.c` | 计算器页（页面+绘制+按键处理+运算逻辑） | **GBK** |
| `src/ui/Driver/Hardware_Driver/OLED_UI_Driver.c/.h` | 新增 `Key_GetRawKey()` 接口（返回键码 1~16） | **UTF-8** |
| `src/ui/Driver/Hardware_Driver/IR_Remote.c/.h` | 遥控数字键 0-9、`*`、`#` 的逻辑映射 | **UTF-8** |

> ⚠️ **文件编码不同**：MenuData.c 是 GBK，两个驱动文件是 UTF-8——**AI 改哪个文件就用哪个编码**，改完验证中文没乱码（§5 第 3 项）。

**零补字**：计算器界面只用数字/运算符（ASCII），**不需要补中文字模**——但页面菜单项如果需要标题文字（如"计算器"），用现有字模（"计/算/器"已在 16×16 磁贴字库）。

---

## 2. 环境速通（10 分钟，只需一次）

### 2.1 切换到虚拟屏模式

打开 `src/inc/config.h`，把：

```c
#define VIRTUAL_OLED    0
```

改成 `1`（保存）。**你开发期间一直保持 1**。

### 2.2 构建（每次改完代码都执行）

回到 Keil 5 进行构建（用 Keil 打开 `Project.uvproj`，点工具栏 **Rebuild（重建）** 按钮，看底部输出窗口）
> 命令行构建也可以（更快）：见 HANDOFF.md §5——注意命令里的 Keil 路径 `D:/APP/C51V961` 按你自己电脑的安装位置修改

**成功**：看到 `0 Error(s)` 和 `Program Size: ...`。
**失败**：把 `build_log.txt` 里的报错整段复制给 AI，让它修。

### 2.3 烧录 + 开虚拟屏

1. 打开 **STC-ISP**，载入 `Objects\Project.hex`，下载到实验箱
2. 打开 **STC-ISP → 工具 → 调试仿真接口 → OLED-128*64** 窗口
3. 画面实时显示在这个窗口里（**这个窗口必须一直开着**——关了 MCU 会等 USB 而死等）

---

## 3. 验收标准（先看目标长什么样）

```
┌────────────────────────────┐
│                            │
│       12+34×5|             │  ← 表达式行（8×16 居中，| 是输入光标）
│       = 182.00             │  ← 结果行（8×16 居中）
│                            │
└────────────────────────────┘
```

- **无按钮网格**——纯按键输入：按实验箱物理键 = 输入数字/运算符
- 表达式行：完整表达式 + 光标（`12+34×5|`），按 `=` 结算；**超 15 字符自动左滚**（只显示末尾 15 字符，新输入从右侧进入）
- 光标附加**闪烁效果**
- 结果行：`= 182.00`（2 位小数）；Error 时**反色显示 `Error`**
- 交互：`12 [+] 34 [×] 5 [=]` → 182.00；`7 [÷] 3 [=]` → 2.33；除零 → Error
- **C 键分级清除**：第一按清除正在输入的数字；再按清除整个表达式+结果

**按键功能分配（16 键）**：数字 0-9（10 键）+ 运算符 `+ − × ÷`（4 键）+ `C`（清除）+ `=`（结算）——正好 16 键。
**遥控映射**：0-9=数字、`*`=×、`#`=÷、▲=+、▼=−、◀=C、OK==、**▶=返回**（◀ 已用作 C，返回用 ▶）。

---

## 4. 给 AI 的任务书

**第一步（最重要）：先做"读码页"建立键码映射表**

计算器要响应全部 16 个物理键，但当前框架只认 4 个键（上/下/确认/返回），**而且实验箱 SW 丝印编号 ≠ 键码（ADC 档位）**——必须先实测每个物理键的键码。让 AI 先做：

```
任务 1（读码页）：在 OLED_UI_MenuData.c 加一个临时页面（或直接改主界面显示）：
按任意物理键 → OLED 显示当前键码（1~16，无键显示 0）。
需要新增接口 Key_GetRawKey()（见下）。
你在实验箱上按遍 16 个键（含方向键区），记录"物理位置 → 键码"表，
回填给 AI，AI 把它写成 Calc_KeyMap[17] 数组（下标=键码，值=按钮 ID）。
```

按钮 ID 定义：`0~9=数字、10=+、11=−、12=×、13=÷、14=C、15==、16=无`

**把下面整段复制给 AI**（`# ===` 之间的内容，任务 1 做完后再给任务 2）：

```
# === 任务书开始 ===

你是 STC8H8K64U 单片机的 C51 嵌入式工程师。项目是"电子手表风格 UI 菜单系统"
（OLED_UI C51 移植框架，Keil C51 编译，显示走虚拟 OLED）。
你的任务：实现计算器页面。所有代码 C51 兼容（C89 子集）。

## 背景（必须先了解）
- 工程：Project.uvproj；菜单数据 src/ui/OLED_UI/OLED_UI_MenuData.c（GBK 编码）
- 驱动：src/ui/Driver/Hardware_Driver/OLED_UI_Driver.c/.h（UTF-8 编码）
- 红外：src/ui/Driver/Hardware_Driver/IR_Remote.c/.h（UTF-8 编码）
- 框架：MenuItem/MenuPage（MENU_TYPE_LIST）；计算器磁贴 MainMenuItems[4]（"计算器"）当前子菜单 NULL
- 显示 API：OLED_ShowString / OLED_ShowNum / OLED_ReverseArea（反色）
- 时钟：GetTick()（毫秒）；键盘驱动已有 16 键 ADC 三态滤波（键码 1~16 存在内部变量）

## 任务 1（必须先完成）：新增 Key_GetRawKey() 接口
在 OLED_UI_Driver.c/.h 新增：
  uint8_t Key_GetRawKey(void);   // 返回当前按下的原始键码 1~16，无键返回 0
实现：直接返回键盘滤波后的当前键值（复用现有逻辑，如 Key_Hold 变量，
若为 KEY_ADC_* 占位映射前的原始键码则直接返回）。保持 UTF-8 编码。

## 任务 2：实现计算器页（MenuData.c）
### 界面（128×64 屏，无按钮网格）
1. 表达式行：8×16 ASCII 居中（y=20），显示完整表达式 + 光标"|"，
   居中公式 x=(128-字符串像素宽)/2（每字符 8px）
2. 结果行：8×16 ASCII 居中（y=40），显示 "= xx.xx"（2 位小数）
3. Error：结果行整行反色（OLED_ReverseArea），内容 "Error"
### 页面结构
- MenuItem CalcMenuItems[] = { {NULL} }（0 个菜单项，{NULL} 直接结尾！）
- MenuPage CalcMenuPage：MENU_TYPE_LIST，父页面 &MainMenuPage，
  General_ShowAuxiliaryFunction = 每帧回调，List_MenuArea 任意（0 项无显示）
- 主界面"计算器"磁贴 child → &CalcMenuPage
### 按键处理（每帧回调里）
- 键盘：Key_GetRawKey() → Calc_KeyMap[键码] → 按钮 ID
  （Calc_KeyMap[17] 数组，下标=键码 1~16，值=按钮 ID，见用户提供的实测表）
- 遥控：IR 逻辑键新增映射（任务 3）——遥控键 → 按钮 ID 直接判断
- 按钮 ID：0~9=数字、10=+、11=−、12=×、13=÷、14=C、15==
- 按键去重：只在"键码从无到有"的边沿触发（上一帧键码==0 才处理）
- 返回：键盘键码 4（若实测返回键=键码 4，否则用实测值）→ 调 OLED_UI_Back()
  遥控 ▶ → 同样返回（◀ 是 C 键，OK 是 = 键）
### 运算模型（定点 2 位小数，int32）
- 输入：整数 0~9999（4 位上限），定点值 = 输入×100
- 表达式：字符串任意长度，**显示时自动左滚**（只显示末尾 15 字符，
  新输入从右侧进入；表达式缓冲建议 40 字符上限，超限忽略后续输入）
- 负号：仅表达式开头（如 -5+3）或运算符后二元减；不支持"运算符后一元负号"
- 结算（按 =）：两遍扫描——先算所有 ×÷（左到右），再算所有 +−（左到右）
  中间结果同样定点（×100）
- 除法：定点除法 a÷b = (a*100)/b（a,b 为定点值；先判断 b==0 → Error）
- 溢出检查：乘法前 if(a>0 && b>2147483647L/a) → Error
  加减法结果超出 ±2147483647L → Error
- 不完整表达式按 =（如 "12+"）→ Error
- 连续运算：= 后结果作为新操作数，再按运算符可继续（如 =后 [+] 5 [=]）
- C 键分级：第一次按 C → 清除正在输入的数字（回到上个运算符之后）；
  第二次按 C → 清除整个表达式+结果
- Error 状态：任何 Error 后按 C 恢复（此时 C 直接全清）
### C51 铁律（违反会编译失败）
- 变量声明放在函数开头；for 初始化区不能声明变量
- 全局/static 变量必须显式初始化（= 0）
- 结构体初始化用位置初始化
- 长整型用 long（C51 的 long 是 32 位）；无 long long
### 文件编码（重要！）
- MenuData.c 是 GBK：改完保持 GBK 编码
- OLED_UI_Driver.c/.h、IR_Remote.c/.h 是 UTF-8：改完保持 UTF-8
- 改完告诉我每个文件保持了正确编码

## 任务 3：遥控数字键映射（IR_Remote.c/.h）
- 现有 IR 逻辑键（IR_KEY_UP/DOWN/LEFT/RIGHT/OK 等）保留
- 新增：遥控数字键 0-9、*、# 的识别——在 IR 键码→逻辑键的映射函数里
  增加分支：数字键返回新逻辑键 IR_KEY_0~IR_KEY_9、IR_KEY_STAR(*)、IR_KEY_HASH(#)
- 计算器页读取：Key_GetIRKey()（或现有接口扩展）返回上述逻辑键

## 完成后
1. 构建 0 Error（L15 警告可忽略）
2. 告诉我：改了哪些文件哪些位置、新增函数清单、构建结果
3. 不要提交 git（用户自己提交）

# === 任务书结束 ===
```

---

## 5. 监督清单（AI 每轮改完你检查什么）

| # | 检查项 | 方法 |
|---|---|---|
| 1 | 只改了允许的 4 个文件 | 让 AI 展示改动文件列表 |
| 2 | 构建 0 Error | 跑 §2.2 命令 |
| 3 | 中文没乱码 | 打开 MenuData.c（GBK）和两个驱动文件（UTF-8）看注释是否正常 |
| 4 | 代码特征抽查 | 让 AI 展示：`Key_GetRawKey` 函数、`Calc_KeyMap[17]` 数组、两遍扫描结算函数 |
| 5 | 读码页结果 | 你实测的 16 键码表已写进 Calc_KeyMap |

---

## 6. 验证清单（虚拟屏上逐项打勾）

| # | 自测项 | 操作 | 通过标准 |
|---|---|---|---|
| 1 | 进入计算器 | 主界面按"计算器"磁贴 | 渐隐进入，页面显示 |
| 2 | 数字输入 | 按物理数字键 | 表达式行显示数字+光标 |
| 3 | 运算符输入 | 按 + − × ÷ | 表达式追加运算符 |
| 4 | 基本运算 | `12+34=` | 结果 `= 46.00` |
| 5 | 优先级 | `12+34×5=` | 结果 `= 182.00`（不是 230） |
| 6 | 除法定点 | `7÷3=` | 结果 `= 2.33` |
| 7 | 负数 | `-5+3=` | 结果 `= -2.00` |
| 8 | 除零 | `5÷0=` | 反色显示 `Error` |
| 9 | 连续运算 | `12+34=` 再 `+5=` | `= 51.00` |
| 10 | C 分级清除 | 输入 `123+456` 按 C → 再按 C | 第一按清掉 456（剩 `123+`）；第二按全部清空 |
| 10b | 长表达式左滚 | 连续输入超 15 字符 | 新输入从右侧进入，显示末尾 15 字符 |
| 11 | 返回 | 按返回键/遥控 ▶ | 返回主界面 |
| 12 | 反复进出 5 次 | — | 不卡死、不空白 |

**出现异常 → §7 处理。**

---

## 7. Bug 处理流程

```
现象（截图/描述）→ 复制给 AI："出现 XX 现象，屏幕显示 XX，请分析原因并修复"
→ AI 改代码 → 你重新构建 → 虚拟屏验证 → 循环
```

常见问题对照表：

| 现象 | 常见原因 | 让 AI 做什么 |
|---|---|---|
| 编译报错 C141/C248 | C51 声明位置/结构体初始化违规 | 按报错行号修，遵守 C51 铁律 |
| 按数字键没反应 | 键码映射表错误/去重逻辑问题 | 重新跑读码页核对 Calc_KeyMap |
| 计算 12+34×5=230 | 没实现优先级（顺序算） | 检查两遍扫描逻辑 |
| 结果小数位不对 | 定点转换错误 | 检查 ×100 时机 |
| 显示 Error 但没报错 | 溢出检查误判 | 检查 2147483647L/a 边界 |
| 中文乱码（菜单全乱） | GBK 被破坏 | git 还原 MenuData.c，让 AI 用 python 按 GBK 处理 |
| 虚拟屏没画面 | STC-ISP 窗口没开/没烧录 | 检查 §2.3 |
| L15 警告 | 框架正常现象 | 忽略 |

**⚠️ 危险动作**（一旦出现立即 git 还原再继续）：
- AI 改了 4 个文件以外的文件 → `git checkout -- <文件名>` 还原
- MenuData.c 中文乱码 → `git checkout -- src/ui/OLED_UI/OLED_UI_MenuData.c` 还原
- 建议：**每次让 AI 动手前，先提交一次 git**（§9）——随时能回滚

---

## 8. 内存监控

每次构建看 `Program Size`：

```
Program Size: data=15.5  xdata=3808  const=6528  code=42760
```

- `code`：当前 42760（48.1KB/64KB）。**计算器完成后应在 44800 以内**（预计 +1.7KB 左右；表达式/映射表都是小数据）
- `xdata`：当前 3808/8192。计算器页约 +30B（表达式缓冲+状态）
- 超限（code > 63000 或 xdata > 8100）→ 立即告诉主开发

---

## 9. git 操作

**每次 AI 改完一版、验证通过后提交一次**（VSCode 终端）：

```bash
git add src/ui/OLED_UI/OLED_UI_MenuData.c \
        src/ui/Driver/Hardware_Driver/OLED_UI_Driver.c src/ui/Driver/Hardware_Driver/OLED_UI_Driver.h \
        src/ui/Driver/Hardware_Driver/IR_Remote.c src/ui/Driver/Hardware_Driver/IR_Remote.h
git commit -m "feat: 计算器页（第N版）——XX 功能正常"
git push origin feature/calc
```

- 只提交上面 5 个源文件（**不要提交 hex/其他文件**）
- 提交信息格式：`feat: ...`（功能）/ `fix: ...`（修复）
- **分支：必须在 `feature/calc` 上**（§0 分支铁律）——提交前 `git branch` 确认，不在就 `git checkout feature/calc`

---

## 10. 完成后

验证清单全过 → 在群里告诉主开发"计算器完成" → 主开发负责：构建复查、实体屏验证、合入 dev。你的任务结束。

---

## 附录 A：为什么不用 MDU16（硬件乘除法器）

STC8H8K64U 内置 MDU16（16 位硬件乘除法器，官方 46 号例程）。**计算器不用它**，原因：
- MDU16 输入是 16 位（≤32767）
- 本计算器是定点 ×100 运算：4 位输入（9999）定点值 = 999900，**超 16 位范围**
- 直接截断会丢精度。32 位 long 运算完全够用（乘法溢出已检查）
- 答辩可讲：定点运算 + 溢出保护（而不是 MDU16）

## 附录 B：测试用例（给 AI 的 /tdd 用）

让 AI 写表达式解析的测试时，用这些已知结果验证（定点 ×100，两遍扫描）：

| 表达式 | 期望（数学值） | 说明 |
|---|---|---|
| `12+34×5` | 182.00 | 优先级：× 先算 |
| `7÷3` | 2.33 | 定点除法取整 |
| `5÷2` | 2.50 | 2 位小数 |
| `-5+3` | -2.00 | 负数 |
| `20×4+10` | 90.00 | × 先算 |
| `6÷2×3` | 9.00 | 同级左到右 |
| `100÷0` | Error | 除零 |
| `9999×9999` | Error | 溢出（数学 99980001 > 定点上限 21474836.47） |
| `123+456-78` | 501.00 | 多操作数 |
